const translations = {
  en: {


        "documents.sourceChoiceAria": "Choose a document source",
        "documents.sourceFile": "Upload file",
        "documents.sourceUrl": "Enter URL",
        "documents.indexFile": "Index file",
        "documents.indexUrl": "Index URL",
        "documents.indexing": "Indexing...",
        "chat.startSessionHint": "Start a new session to chat with the assistant.",
        "shell.navLearning": "Learning",
        "shell.contact": "Contact",
        "account.showPassword": "Show password",
        "account.hidePassword": "Hide password",
        "account.provisioningInfo": "Accounts are issued by the school. Please contact the school to request an account.",
        "chat.star": "Star",
        "chat.unstar": "Unstar",
        "chat.rename": "Rename",
        "chat.delete": "Delete",
        "confirm.title": "Confirm action",
        "confirm.cancel": "Cancel",
        "confirm.accept": "Continue",
        "packages.learningPackage": "Learning packages",
        "packages.viewAvailable": "View available packages",
        "packages.buyPackage": "Buy package",
        "packages.free": "Free",
        "packages.discount": "Save",
        "packages.studentRecommended": "Recommended for students",
        "packages.profileEffect": "Special profile effect",
        "packages.prioritySupport": "Priority support with faster responses",
        "packages.cannotDowngrade": "Cannot downgrade",
        "packages.lifetimeNoDowngrade": "A lifetime package cannot be downgraded.",
        "packages.downgradeAfter": "You can choose a lower package after",
        "packages.processing": "Processing...",
        "loading.page": "Loading content...",
        "packages.chooseSuitable": "Choose a suitable package",
        "packages.lifetimeActive": "Active forever",
        "packages.remaining": "Remaining",


        
        






        
        
        "documents.addDocBtn": "Add document",
        "documents.noDocsInChapter": "No documents in this chapter yet.",
        
        "chat.replyPrefsAria": "Reply preferences",
        "chat.speed": "Display speed",
        "chat.speedHelp": "Typing effect",
        "chat.depthHelp": "Adjust answer detail",
        "chat.accuracy": "Accuracy",
        "chat.accuracyDesc": "Recommended for students, ensures information is strictly based on documents with clear citations.",
        
        "admin.confirmRemoveSubjectStudent": "Remove subject from student?",
        "admin.confirmRegisterSubjectStudent": "Register selected subject for student?",
        "admin.registerSubjectStudentAria": "Register subject for student",
        "admin.documents": "Documents",
        
        "subscriptions.noActivePackage": "No active package",
        "subscriptions.choosePackageDesc": "Choose a suitable package to start document-based Q&A.",
        "subscriptions.viewPackagesBtn": "View packages",
        "subscriptions.startDate": "Start date",
        "subscriptions.status": "Status",
        "subscriptions.clickToView": "Click to view",
        "subscriptions.recentTransactions": "recent transactions",
        "subscriptions.noTransactions": "No transactions yet",
        "subscriptions.receiptDesc": "Receipts will appear here after you pay or activate a package.",
        "subscriptions.receiptTitle": "PAYMENT RECEIPT",
        "subscriptions.buyer": "Buyer",
        "subscriptions.transactionId": "Transaction ID",
        "subscriptions.amount": "Amount",
        "subscriptions.paymentDate": "Payment date",
        "subscriptions.paymentMethod": "Method",
        "subscriptions.purchasedPackage": "Purchased package",
        "subscriptions.printReceipt": "Print receipt",


        "admin.userTableHeading": "User table",
        "admin.assignedCannotRemove": "Assigned - cannot remove",
        "admin.confirmDeleteUser": "Delete this user? This cannot be undone.",
        "admin.assignedClickRemove": "Assigned - click X to remove",
        "admin.confirmRemoveSubject": "Remove subject from lecturer?",
        "admin.removeSubject": "Remove subject",
        "admin.confirmRegisterSubject": "Register selected subject for lecturer?",
        "admin.registerSubjectAria": "Register subject for lecturer",
        "admin.overviewAria": "Overview metrics",
        "admin.chatSessions": "Chat sessions",

        "documents.backToRepo": "Back to repository",
        "documents.editInfoTitle": "Edit document info",
        "documents.fileType": "File type",
        "documents.fileSize": "Size",
        "documents.displayName": "Display name",
        "documents.displayNameDesc": "Does not change physical file; only changes display name and future citations.",
        "documents.saveChanges": "Save changes",
        "documents.order": "Order",
        
        "packages.title": "Choose the right learning package",
        "packages.desc": "Upgrade your Q&A limit to learn more effectively with course materials.",
        "packages.viewCurrent": "View current package",
        "packages.currentPackageAria": "Current package",
        "packages.currentPackage": "Current package",
        "packages.validUntil": "Valid until",
        "packages.activeBadge": "Active",
        "packages.inUseBadge": "In use",
        "packages.popularBadge": "Popular",
        
        "payment.resultEyebrow": "Transaction result",
        
        "shell.openAccountMenu": "Open account menu",
        "shell.accountNav": "Account",
        "shell.loggedInAs": "Logged in as",
        "shell.packages": "Packages",
        "shell.statistics": "Statistics",
        
        "subscriptions.profileAndBilling": "Profile and billing",
        "subscriptions.manageAccount": "Manage your account",
        "subscriptions.manageDesc": "Update display name, track learning package, and review transaction receipts.",
        "subscriptions.explorePackages": "Explore packages",
        "subscriptions.buyerInfo": "Buyer information",
        "subscriptions.buyerInfoDesc": "This name appears on the account and receipts.",
        "subscriptions.currentPackage": "Current learning package",


        "account.changePasswordTitle": "Change Password",
        "account.security": "Account Security",
        "account.currentPassword": "Current password",
        "account.newPassword": "New password",
        "account.confirmNewPassword": "Confirm new password",
        
        "account.forgotPassword": "Forgot Password",
        "account.recovery": "Account Recovery",
        "account.forgotPasswordDesc": "Enter your account email to receive a password reset link.",
        "account.sendLink": "Send link",
        "account.rememberedPassword": "Remembered password?",
        "account.login": "Login",
        
        "account.resetPassword": "Reset Password",
        "account.passwordResetEyebrow": "Password Reset",
        "account.resetPasswordDesc": "Create a new password for your account.",
        "account.backToLogin": "Back to login",
        
        "account.loginTitle": "Login",
        "account.loginDesc": "Access document repository, manage indexed content and continue source-based Q&A.",
        "account.password": "Password",
        "account.forgotPasswordLink": "Forgot password?",
        "account.loginGoogle": "Login with Google",
        "account.noAccountContact": "Don't have an account? Please contact the school to request one.",
        
        "account.createAccountTitle": "Create Account",
        "account.newAccountEyebrow": "New Account",
        "account.createAccountDesc": "Create an internal account to manage documents and use Q&A sessions.",
        "account.fullName": "Full name",
        "account.confirmPassword": "Confirm password",
        "account.alreadyHaveAccount": "Already have an account?",


        "account.profile": "User profile",
        "account.profileDesc": "Edit display name",
        "account.packagesDesc": "View and choose a learning package",
        "account.history": "Transaction history",
        "account.historyDesc": "View and print receipts",
        "account.changePassword": "Change password",
        "account.changePasswordDesc": "Protect your account",
        "account.logout": "Logout",
        "admin.navAiSettings": "AI Settings",
        "aiSettings.title": "AI Learning Assistant Settings",
        "aiSettings.eyebrow": "How the assistant reads and answers",
        "aiSettings.desc": "Adjust answer quality and how documents are chunked for retrieval.",
        "aiSettings.answerMethod": "Answer generation",
        "aiSettings.answerMethodDesc": "Choose the engine used to understand questions and write answers.",
        "aiSettings.answerModel": "Answer engine",
        "aiSettings.answerModelHelp": "Example: gemini-3.5-flash. Only change when provider confirms new name.",
        "aiSettings.readMethod": "Document reading",
        "aiSettings.readMethodDesc": "This setting affects the ability to find correct content in documents.",
        "aiSettings.readModel": "Reading engine",
        "aiSettings.readDetail": "Detail level for memorization",
        "aiSettings.readDetailHelp": "768 is suitable for most course materials.",
        "aiSettings.chunkMethod": "Content chunking",
        "aiSettings.chunkMethodDesc": "Short chunks search faster; long chunks retain more context.",
        "aiSettings.chunkLength": "Length per chunk",
        "aiSettings.chunkLengthHelp": "Recommended: 800–1,200 characters.",
        "aiSettings.chunkOverlap": "Overlap between chunks",
        "aiSettings.chunkOverlapHelp": "Recommended: 50–150 characters to not lose meaning.",
        "aiSettings.reindexNote": "Note: previously processed documents need to be re-indexed to use new settings.",
        "aiSettings.save": "Save settings",

    "nav.documents": "Documents",
    "nav.chat": "Chat",
    "nav.logout": "Logout",
    "nav.login": "Login",
    "nav.register": "Create account",
    "nav.billing": "Subscriptions",
    "nav.reports": "Reports & Statistics",
    "nav.reports.overview": "Overview",
    "nav.reports.chatbot": "Chatbot",
    "nav.reports.documents": "Documents",
    "nav.reports.users": "Users",
    "nav.reports.permissions": "Permissions",
    "nav.reports.subscription": "Packages",
    "nav.reports.export": "Export Report",
    "stat.timeRange": "Time range",
    "stat.7days": "7 days",
    "stat.30days": "30 days",
    "stat.90days": "90 days",
    "stat.custom": "Custom",
    "stat.exportReport": "Export report",
    "stat.chatQuestions": "Chat questions",
    "stat.chatSessions": "Chat sessions",
    "stat.activeStudents": "Active students",
    "stat.documents": "Documents",
    "stat.totalDocsInRepo": "Total docs in repo",
    "stat.indexed": "Indexed:",
    "stat.percentTotalDocs": "% of total docs",
    "stat.basedOnAssistant": "Based on assistant answers",
    "stat.totalQuestions": "Total questions",
    "stat.totalSessions": "Total sessions",
    "stat.lowFeedback": "Low Feedback",
    "stat.noRatingData": "No star rating data yet",
    "stat.responseTime": "Response Time",
    "stat.noLatencyData": "Latency not recorded",
    "stat.chatbotFilters": "Chatbot Filters",
    "stat.chatbotFiltersDesc": "Preparing UI to filter by date, subject, status and feedback.",
    "stat.noData": "No data",
    "stat.usersReport": "Users Report",
    "stat.usersReportDesc": "Analyze user growth and active rates.",
    "stat.students": "Students",
    "stat.lecturers": "Lecturers",
    "stat.admins": "Admins",
    "stat.subscriptionsReport": "Subscriptions Report",
    "stat.subscriptionsReportDesc": "Analyze revenue and service packages subscribed by students.",
    "stat.revenue": "Revenue",
    "stat.vnd": "VND",
    "stat.payments": "Payments",
    "stat.successfulPayments": "Successful payments",
    "stat.newSubscriptions": "New subscriptions",
    "stat.pendingPayments": "Pending payments",
    "stat.revenueLast30Days": "Revenue last 30 days",
    "stat.paidPackages": "Paid packages",
    "stat.pendingPackages": "Pending packages",
    "stat.canceledPackages": "Canceled packages",
    "stat.subscriptionAllocation": "Subscription Allocation",
    "stat.thServicePackage": "Service Package",
    "stat.thUsers": "Users",
    "stat.thProportion": "Proportion",
    "stat.freePackage": "Free Package",
    "stat.proPackage": "Pro Package",
    "stat.proMaxPackage": "Pro Max Package",
    "stat.thereAreCurrently": "There are currently",
    "stat.newSubsInFilter": "new subscriptions in the filter range.",
    "stat.pendingPaymentsAccountFor": "Pending payments account for",
    "stat.totalTransactions": "of total transactions.",
    "shell.portal": "Learning Portal",
    "shell.chatbot": "Chatbot",
    "shell.courses": "Courses",
    "shell.documents": "Document Repository",
    "shell.users": "Users",
    "shell.help": "Help",
    "shell.search": "Search the system...",
    "shell.notifications": "Notifications",
    "shell.history": "History",
    "shell.changePassword": "Change password",
    "admin.totalUsers": "Total users",
    "admin.usersShown": "shown",
    "admin.admins": "Admins",
    "admin.fullSystemAccess": "Full system access",
    "admin.manageSubjects": "Manage assigned subjects",
    "admin.readChatAccess": "Read and chat access",
    "admin.subjects": "Subjects",
    "admin.subjectsAssigned": "assigned",
    "admin.directory": "Directory",
    "admin.searchDirectory": "Search / Tìm kiếm",
    "admin.searchDirectoryPlaceholder": "Name, email, provider, subject...",
    "admin.roleFilter": "Role / Vai trò",
    "admin.filter": "Filter",
    "admin.clear": "Clear",
    "admin.thCreated": "Created",
    "admin.allIndexedDocs": "All indexed documents",
    "admin.subjectLeaderSuffix": " (Subject leader)",
    "admin.leaderRole": "Subject leader",
    "admin.roleStudent": "Student",
    "admin.roleLecturer": "Lecturer",
    "admin.roleAdmin": "Admin",
    "documents.backToCatalog": "Back to course list",
    "documents.defaultDescription": "Manage chapters, files and lecture links for this course.",
    "documents.documentsUnit": "documents",
    "documents.chaptersUnit": "chapters",
    "documents.editSubjectInfo": "Edit course information",
    "documents.subjectCode": "Course code",
    "documents.subjectDesc": "Description",
    "documents.deleteSubject": "Delete course",
    "documents.saveSubject": "Save course",
    "documents.addChapter": "Add chapter",
    "documents.chapterTitlePlaceholder": "Ex: Chapter 1 / Week 1",
    "documents.sortOrder": "Sort order",
    "documents.addChapterBtn": "Add chapter",
    "documents.addDocument": "Add document",
    "documents.documentFile": "Document file",
    "documents.orLectureUrl": "Or lecture URL",
    "documents.indexDocumentBtn": "Index document",
    "documents.chaptersAndDocuments": "Chapters and documents",
    "documents.noChaptersHint": "Add a chapter first, then upload documents into the correct category.",
    "documents.updateBtn": "Update",
    "documents.deleteBtn": "Delete",
    "documents.addFileTo": "Add file to ",
    "documents.chapterLabel": "Chapter",
    "documents.emptyChapter": "No documents in this chapter.",
    "documents.previewBtn": "Preview",
    "documents.editBtn": "Edit",
    "documents.reindexBtn": "Re-index",
    "documents.deleteDocBtn": "Delete",
    "documents.indexedContent": "Indexed content",
    "documents.previewTitle": "Document",
    "documents.loadingContent": "Loading indexed content...",
    "documents.reindexConfirm": "Re-index this document?",
    "documents.deleteConfirm": "Delete this document from the index? This action cannot be undone.",
    "documents.deleteChapterConfirm": "Delete this chapter? This action cannot be undone.",
    "documents.deleteSubjectConfirm": "Delete this course and all its chapters? This action cannot be undone.",
    "documents.catalogEmptyDesc": "List of created chapters",
    "documents.emptyCatalogTitle": "No course catalog yet.",
    "documents.emptyCatalogHint": "Create a course first, then add chapters to upload documents according to standard categories.",
    "documents.createFirstChapterBtn": "Create first chapter",
    "documents.docInfo": "Document information",
    "documents.chooseFileBtn": "Choose file",
    "documents.enterUrl": "Enter lecture URL",
    "admin.usersTitle": "Users",
    "admin.navDirectory": "Directory",
    "admin.navOnlineUsers": "Online users",
    "admin.navLecturerTable": "Lecturer table",
    "admin.navStudentTable": "Student table",
    "admin.navImportExcel": "Import from Excel",
    "admin.navSubjectManagement": "Subject management",
    "admin.thStudents": "STUDENTS",
    "admin.deactivate": "Deactivate",
    "admin.activate": "Activate",
    "courses.eyebrow": "Course workspace",
    "courses.description": "Open a course workspace to view documents, chapters, and subject chat.",
    "courses.emptyTitle": "No course workspace is available.",
    "courses.emptyHint": "Upload and index course documents first, then come back here.",
    "courses.noDescription": "No description yet.",
    "courses.unassigned": "Unassigned",
    "courses.chaptersUnit": "chapters",
    "courses.eyebrowAdmin": "Content management",
    "courses.eyebrowTeacher": "Teaching workspace",
    "courses.titleAdmin": "Course overview",
    "courses.titleTeacher": "My courses",
    "courses.descAdmin": "Monitor document coverage and open each course for detailed checks.",
    "courses.descTeacher": "Prioritize courses lacking documents, then open the workspace to manage chapters and test the chatbot.",
    "courses.btnAdmin": "Manage catalog",
    "courses.btnTeacher": "Upload document",
    "courses.kpiCourses": "Visible courses",
    "courses.kpiChaptersUnit": "chapters",
    "courses.kpiReadiness": "Content readiness",
    "courses.kpiIndexedUnit": "indexed documents",
    "courses.kpiAttention": "Needs attention",
    "courses.kpiAllReady": "All courses ready",
    "courses.kpiNeedsAttention": "Missing or failed content",
    "courses.statusMissing": "Missing documents",
    "courses.statusReady": "Ready",
    "courses.statusAttention": "Needs attention",
    "courses.indexedStatus": "indexed documents",
    "courseWorkspace.backToCourses": "Back to courses",
    "courseWorkspace.defaultDescription": "Course workspace for documents, chapters, and subject chat.",
    "courseWorkspace.chatCourse": "Chat this course",
    "courseWorkspace.manageDocuments": "Manage documents",
    "courseWorkspace.documents": "Documents",
    "courseWorkspace.chapters": "Chapters",
    "courseWorkspace.lecturer": "Lecturer",
    "courseWorkspace.noEmail": "No owner email",
    "courseWorkspace.courseMap": "Course map",
    "courseWorkspace.chaptersAndDocs": "Chapters and documents",
    "courseWorkspace.emptyChaptersTitle": "No chapter has indexed content yet.",
    "courseWorkspace.emptyChaptersHint": "Upload documents or ask the lecturer to add course material.",
    "courseWorkspace.docsUnit": "docs",
    "courseWorkspace.chunksUnit": "chunks",
    "admin.defaultRole": "Default role",
    "admin.defaultLecturerSubjects": "Default subjects for lecturers",
    "admin.confirmImportUsers": "Create these users in bulk and send welcome emails?",
    "admin.confirmRoleChange": "Change {user} to {role}?",
    "admin.roleChangeTitle": "Changing a role asks for confirmation and saves automatically.",
    "onlineUsers.title": "Online users",
    "onlineUsers.connecting": "Connecting...",
    "onlineUsers.loading": "Loading online users...",
    "onlineUsers.empty": "No active users right now.",
    "onlineUsers.activeNow": "Active now",
    "onlineUsers.realtimeNote": "Realtime presence",
    "onlineUsers.statusHelp": "This list updates when users connect or leave.",
    "onlineUsers.summaryOne": "1 online user (active now)",
    "onlineUsers.summaryMany": "{count} online users (active now)",
    "onlineUsers.youAreOnline": "You are online",
    "onlineUsers.online": "Online",
    "onlineUsers.tabs": "{count} tabs",
    "onlineUsers.expand": "Expand online users",
    "onlineUsers.collapse": "Collapse online users",
    "documents.shellTitle": "Document Repository",
    "documents.manageTitle": "Document repository management",
    "documents.manageSubtitle": "Upload learning materials, extract content, and index them so the chatbot can answer with sources.",
    "documents.uploadKicker": "Upload",
    "documents.dropTitle": "Drop documents here",
    "documents.chooseFile": "Choose a file or drag it here",
    "documents.urlRenderHint": "SPA/React/Vue pages will be rendered before DOM extraction.",
    "documents.storage": "Storage",
    "documents.uploadedSize": "Uploaded size",
    "documents.totalUploaded": "Total uploaded",
    "documents.documentList": "Document list",
    "documents.showing": "Showing",
    "documents.fileName": "File name",
    "documents.status": "Status",
    "documents.uploadDate": "Upload date",
    "documents.indexedStatus": "Indexed",
    "documents.emptyTitle": "No documents yet.",
    "documents.emptyHint": "Upload a syllabus or lecture URL to start source-grounded Q&A.",
    "assistant.open": "Open chat page",
    "assistant.hidden": "Hi, I am your chatbot assistant for finding information faster.",
    "documents.title": "Course document repository",
    "documents.subtitle": "Manage uploaded learning materials, extract content, and index them for the dedicated chat page.",
    "documents.openChat": "Open chat",
    "documents.statsAria": "Document repository statistics",
    "documents.statsDocuments": "Documents",
    "documents.statsIndexed": "Indexed",
    "documents.kpiIndexedSub": "Ready for chat",
    "documents.statsProcessing": "Processing",
    "documents.kpiProcessingSub": "Queued or indexing",
    "documents.kpiFailedSub": "Needs attention",
    "documents.uploadTitle": "Upload document",
    "documents.uploadSubtitle": "PDF, DOCX, PPTX, XLSX, TXT, MD, CSV, or a lecture page URL will be extracted, chunked, and embedded automatically.",
    "documents.subject": "Subject",
    "documents.chapter": "Chapter",
    "documents.source": "Source",
    "documents.subjectPlaceholder": "Example: IOT102",
    "documents.chapterPlaceholder": "Example: Chapter 1 or Week 1",
    "documents.dropzoneTitle": "Drag and drop a document or click to choose a file",
    "documents.dropzoneDefault": "Supports PDF, DOCX, PPTX, XLSX, TXT, MD, CSV",
    "documents.or": "or",
    "documents.url": "Lecture page URL",
    "documents.urlPlaceholder": "https://example.com/react-vue-lecture",
    "documents.urlHint": "SPA/React/Vue pages will be rendered with Playwright before DOM extraction.",
    "documents.submit": "Upload and index",
    "documents.indexedTitle": "Indexed documents",
    "documents.filesUnit": "files",
    "documents.empty": "No documents yet. Upload course materials to start asking source-grounded questions.",
    "documents.view": "View",
    "documents.navList": "Document list",
    "documents.navUpload": "Upload & index",
    "documents.navCatalog": "Course catalog",
    "documents.eyebrow": "Learning content operations",
    "documents.kpiTotal": "Total",
    "documents.kpiFailed": "Failed",
    "documents.selectSubject": "Select subject",
    "documents.noSubjectAssigned": "No subjects assigned to this account yet.",
    "documents.reindexStale": "Re-index stale",
    "documents.search": "Search",
    "documents.searchPlaceholder": "File, subject, chapter, uploader...",
    "documents.allSubjects": "All subjects",
    "documents.allStatus": "All status",
    "documents.filter": "Filter",
    "documents.clear": "Clear",
    "documents.uploader": "Uploader",
    "documents.subjectChapter": "Subject / Chapter",
    "documents.chunksSize": "Chunks / Size",
    "documents.chunksUnit": "chunks",
    "documents.unknown": "Unknown",
    "documents.actions": "Actions",
    "documents.edit": "Edit",
    "documents.reindex": "Re-index",
    "documents.delete": "Delete",
    "chat.sessionsAria": "Chat session history",
    "chat.documents": "Documents",
    "chat.newSession": "New session",
    "chat.history": "Session history",
    "chat.sessionsUnit": "sessions",
    "chat.messagesUnit": "messages",
    "chat.noSessions": "No sessions yet.",
    "chat.mainAria": "Document chat",
    "chat.title": "Document chat",
    "chat.subtitle": "Ask questions based on indexed documents. Questions outside the document scope will be marked as insufficient data.",
    "chat.headerKicker": "Course assistant",
    "chat.headerTitle": "Introduction to AI",
    "chat.headerSubtitle": "Ask questions from indexed documents. If the data is insufficient, the chatbot must report missing sources instead of guessing.",
    "chat.currentSession": "Current session",
    "chat.suggestionsAria": "Question suggestions",
    "chat.welcomeNamed": "Hi {name}! I’m your learning assistant. What would you like me to find, summarize, or explain from the documents today?",
    "chat.answerLabel": "Answer",
    "chat.followUpLabel": "You can ask next",
    "chat.placeholder": "Ask about a subject, chapter, or indexed document...",
    "chat.send": "Send",
    "chat.relatedLabel": "Related questions",
    "chat.relatedAria": "Related questions",
    "chat.allSubjects": "All subjects",
    "chat.defaultSessionTitle": "Session without a question",
    "chat.sessionActions": "Session actions",
    "chat.starSession": "Star",
    "chat.unstarSession": "Unstar",
    "chat.renameSession": "Rename",
    "chat.deleteSession": "Delete",
    "chat.renamePrompt": "New session name",
    "chat.deleteConfirm": "Delete this chat session?",
    "chat.sessionActionError": "Could not update the chat session.",
    "chat.loading": "Give me a moment, checking the documents...",
    "chat.requestError": "Could not process the question.",
    "chat.connectionError": "Could not connect to the server. Check the app and try again.",
    "chat.suggestions": [
      "Which subjects have indexed documents?",
      "Summarize the uploaded documents.",
      "What can I ask from the document repository?",
      "Which document should I read first?"
    ],
    "admin.chatQuestionsNote": "Chat questions",
    "stat.noChatDataDesc2": "Students haven't used the chatbot during this period. Try changing the date filter or checking chat data.",
    "admin.dailyChartAria": "Daily chat usage chart",
    "admin.viewDetails": "View details",
    "admin.viewAll": "View all",
    "admin.topSubjectsCaption": "Subjects with most citations in chatbot",
    "admin.noData": "No data yet",
    "admin.noSubjectsQueried": "No subjects were queried during this period.",
    "stat.thSubject": "Subject",
    "stat.thQuestions": "Questions",
    "stat.thDocs": "Documents",
    "stat.thReadiness": "Readiness rate",
    "admin.questionsUnit": "questions",
    "admin.totalQuestions": "Total questions",
    "admin.totalChatSessions": "Total chat sessions",
    "stat.unanswered": "Unanswered questions",
    "stat.needMoreData": "Needs more data",
    "admin.chatbotLimitsAria": "Chatbot data limits",
    "admin.currentMeasurementScope": "Current measurement scope",
    "admin.measurementScopeDesc": "The report tracks question count, sessions, and active users. Feedback and response time are not collected yet.",
    "admin.compactTableDesc": "Compact table format instead of multiple stretched tables.",
    "admin.chatbotDailyUsageCaption": "Chatbot daily usage within report range",
    "admin.thDate": "Date",
    "admin.thChatSessions": "Chat sessions",
    "admin.thActiveUsers": "Active users",
    "admin.totalDocs": "Total documents",
    "admin.inAllSystem": "In entire system",
    "admin.indexed": "Indexed",
    "admin.processing": "Processing",
    "admin.processingDesc": "Chunking/indexing",
    "admin.checkFiles": "Needs file check",
    "admin.subjectsNoDocs": "Subjects without docs",
    "admin.needMoreDocs": "Needs learning materials",
    "admin.recentDocsCaption": "Recently uploaded documents",
    "admin.thDocument": "Document",
    "admin.thUploader": "Uploader",
    "admin.thCitation": "Citation",
    "admin.thUploadDate": "Upload date",
    "admin.unknown": "Unknown",
    "admin.status.indexed": "Indexed",
    "admin.status.processing": "Processing",
    "admin.status.failed": "Failed",
    "admin.students": "Students",
    "admin.chatbotUsageLevel": "Chatbot usage level.",
    "admin.totalStudents": "Total students",
    "admin.activeInDays": "Active in {0} days",
    "admin.mostQuestions": "Most questions",
    "admin.lecturers": "Lecturers",
    "admin.byUploadPerms": "By upload permissions and docs.",
    "admin.totalLecturers": "Total lecturers",
    "admin.activeUploadPerms": "Active upload permissions",
    "admin.avgDocsPerSubject": "Avg docs/subject",
    "admin.topChatUsersNote": "Identify highly active students.",
    "admin.topUsersCaption": "Top users with most questions",
    "admin.thLastQuestion": "Last question",
    "admin.leaderLecturer": "Lead lecturer",
    "admin.assignedSubjects": "Subjects with assigned leader",
    "admin.uploadPerms": "Upload permissions",
    "admin.assignedBySubject": "Assigned per subject",
    "admin.unassignedSubjects": "Unassigned subjects",
    "admin.needsLeader": "Needs lead lecturer",
    "admin.assignLeaderBtn": "Assign lead lecturer",
    "admin.leaderStatusCaption": "Lecturer assignment status",
    "admin.thLeaderSubject": "Lead subject",
    "admin.thUploadPerms": "Upload perms",
    "admin.unassigned": "Unassigned",
    "admin.yes": "Yes",
    "admin.notYet": "Not yet",
    "admin.status.needsAssign": "Needs assign",
    "admin.status.active": "Active",
    "admin.aiEyebrow": "How assistant reads and replies",
    "admin.aiSettingsTitle": "Assistant settings",
    "admin.aiSettingsDesc": "Configure how the assistant reads documents and generates answers.",
    "admin.genAnswerTitle": "Generate answer",
    "admin.genAnswerDesc": "Select AI service and engine to comprehend questions and write answers.",
    "admin.aiService": "AI Service",
    "admin.apiKeyProtected": "Connection key is secured in server config and hidden here.",
    "admin.answerEngine": "Answer engine",
    "admin.geminiDefaultGroqSpeed": "Gemini is good by default; Groq is high speed for RAG.",
    "admin.provider.configured": "Service connected",
    "admin.provider.missing": "Service missing connection key",
    "admin.readDocTitle": "Read documents",
    "admin.readDocDesc": "Choose how the assistant memorizes content to find related chunks.",
    "admin.readEngine": "Reading engine",
    "admin.detailLevel": "Memorization detail level",
    "admin.detailLevelDesc": "Default 768. Higher levels keep more detail but increase size and search time.",
    "admin.chunkDocTitle": "Document chunking",
    "admin.chunkDocDesc": "Large files are split into chunks. Short chunks aid precise search; long ones keep context.",
    "admin.maxChunkLength": "Max chunk length (chars)",
    "admin.chunkLengthDesc": "Recommended: 800-1200 characters.",
    "admin.chunkOverlap": "Overlap between chunks",
    "admin.chunkOverlapDesc": "Prevents missing context at cut points. Recommended ~10% of chunk length.",
    "admin.docSimulation": "Document simulation...",
    "admin.chunk1": "Chunk 1",
    "admin.chunk2": "Chunk 2",
    "admin.overlapTooltip": "Overlap section",
    "admin.note": "Note:",
    "admin.chunkWarning": "If you change chunking parameters, previously uploaded docs must be re-embedded.",
    "admin.saveConfig": "Save system configuration",
    "admin.groqHint": "Recommended llama-3.3-70b-versatile for quality or llama-3.1-8b-instant for speed.",
    "admin.geminiHint": "Recommended gemini-3.5-flash for balanced speed and cost.",
    "shell.assistantSettings": "Assistant Settings",
    "shell.reports": "Reports & Statistics",

    "documents.status.indexed": "Indexed",
    "documents.status.processing": "Processing",
    "documents.status.failed": "Failed",
    "documents.status.unknown": "Unknown",
    "documents.failedTooltip": "System error processing document. Please check the file or delete and reupload.",


    "chat.responsePaceAria": "Response pace",
    "chat.paceFast": "Fast",
    "chat.paceComfortable": "Comfortable",
    "chat.replyMode": "Answer detail",
    "chat.answerDepthAria": "Assistance depth",
    "chat.depthConcise": "Concise",
    "chat.depthBalanced": "Balanced",
    "chat.depthDeep": "In-depth",
    "chat.remaining": "Remaining",
    "chat.unlimited": "Unlimited",
    
    "packages.days": "days",
    "packages.lifetime": "Lifetime",
    
    "packages.name.free": "Free",
    "packages.desc.free": "Trial package for students to ask the chatbot at a basic level.",
    "packages.name.student": "Student",
    "packages.desc.student": "Monthly learning package for students to ask the chatbot per subject.",
    "packages.name.pro": "Pro",
    "packages.desc.pro": "Advanced package for students who need to ask the chatbot frequently.",
    "packages.name.annual": "12 months",
    "packages.desc.annual": "A full school year with more cost savings compared to monthly renewals.",
    "packages.name.none": "No package",


    "admin.shellTitleUsers": "Users",
    "admin.shellTitleStats": "Reports & Statistics",
    "home.shellTitleChat": "Document chatbot",
    "home.shellTitleWorkspace": "Course workspace",
    "home.shellTitleCourses": "Courses",
    "home.shellTitleDocs": "Document repository",
    "home.shellTitleOnlineUsers": "Online users",
    "home.shellTitleSubjectDocs": "Subject details",
    "packages.shellTitle": "Service packages",
    "payment.shellTitle": "Payment result",
    "subscriptions.shellTitle": "Account and payment",


    "shell.title": "Course Assistant",
    "aiSettings.shellTitle": "Assistant settings",


    "packages.startNow": "start now",
    "packages.oneTimePayment": "one-time payment",
    "packages.for": "for",
    "packages.questionsPerMonth": "questions per month",
    "packages.answerBasedOnDocs": "Answer based on course documents",
    "packages.saveHistory": "Save history and references",
    "packages.duration": "Duration:",
    "packages.activateNow": "Activate now",
    "packages.selectPackage": "Select this package",
    "packages.choosePayment": "Choose payment method",
    "packages.payWithMomo": "Pay via MoMo app",
    "packages.payWithBanking": "Scan QR code with banking app",
    
    "payment.checkInfo": "Need to re-check info",
    "payment.backToPackages": "Back to packages",
    "payment.payVia": "Pay via",
    "payment.orderCode": "Order code",
    "payment.startChat": "Start Q&A",
    "payment.reselectMethod": "Reselect method",
    "payment.checkCurrentPackage": "Check current package",
    
    "documents.chapterTitle": "Chapter",
    "documents.orUrl": "Or lecture URL",
    "documents.addDocToSubject": "Add document to subject",
    "documents.noChapters": "This subject has no chapters yet.",
    "documents.documentsLower": "documents",

    "admin.active": "Active",
    "admin.addAccount": "Add account",
    "admin.allRoles": "All roles",
    "admin.apply": "Apply",
    "admin.assignButton": "Assign",
    "admin.clearFilters": "Clear filters",
    "admin.createAccountHeading": "Create user",
    "admin.createLecturerHint": "Lecturers can manage subjects and learning materials.",
    "admin.createStudentHint": "Students can access chatbot for their assigned subjects.",
    "admin.createSubjectPrompt": "Subject code",
    "admin.createUserBtn": "Create account",
    "admin.delete": "Delete",
    "admin.description": "Description",
    "admin.email": "Email",
    "admin.excelFile": "Excel file",
    "admin.excelImportHint": "Upload a .xlsx file containing user records.",
    "admin.eyebrow": "System Administration",
    "admin.fullName": "Full name",
    "admin.importExcelEyebrow": "Bulk Import",
    "admin.importExcelTitle": "Import from Excel",
    "admin.importUsers": "Import users",
    "admin.importWelcomeNote": "Download template and upload to create multiple accounts at once.",
    "admin.inactive": "Inactive",
    "admin.kpiDeletable": "Can be deleted",
    "admin.kpiDeletableDesc": "No subjects assigned & inactive > 3 months",
    "admin.kpiLecturers": "Lecturers",
    "admin.kpiLecturersDesc": "Managing assigned subjects",
    "admin.kpiStudents": "Students",
    "admin.kpiStudentsDesc": "Using learning assistant",
    "admin.kpiTotalUsers": "Total users",
    "admin.lecturerManagement": "Lecturer Management",
    "admin.lecturersUnit": "lecturers",
    "admin.lecturerTableCaption": "Lecturer assignment table",
    "admin.lecturerTableHeading": "Lecturers & Assignments",
    "admin.navCreateAccount": "Create account",
    "admin.navCreateSubject": "Create subject",
    "admin.noLecturersFound": "No lecturers found.",
    "admin.noMatchingUsers": "No matching users.",
    "admin.noMatchingUsersHint": "Clear filters or create a new account from the command panel.",
    "admin.noneLeader": "No leader",
    "admin.noStudentsFound": "No students found.",
    "admin.noSubjects": "No subjects",
    "admin.noSubjectsAvailable": "No subjects available",
    "admin.notApplicable": "Not applicable",
    "admin.noUnassignedSubjects": "No unassigned subjects",
    "admin.overview": "Overview",
    "admin.password": "Password",
    "admin.role": "Role",
    "admin.role.admin": "Admin",
    "admin.role.lecturer": "Lecturer",
    "admin.role.student": "Student",
    "admin.search": "Search",
    "admin.selectSubjectAssign": "Select a subject to assign...",
    "admin.showing": "Showing",
    "admin.studentAllowed": "Allowed to use assistant",
    "admin.studentManagement": "Student Management",
    "admin.studentsUnit": "students",
    "admin.studentTableCaption": "Student table",
    "admin.studentTableHeading": "Students",
    "admin.subjectCode": "Subject code",
    "admin.subjectList": "Subject List",
    "admin.subjectsForLecturer": "Assigned subjects",
    "admin.subjectsUnit": "subjects",
    "admin.subjectTableCaption": "Subject list",
    "admin.tabCreateAccount": "Add account",
    "admin.tabImportExcel": "Import Excel",
    "admin.tableCaption": "User list by current filter",
    "admin.tabLecturers": "Lecturer Assignment",
    "admin.tabList": "Directory",
    "admin.tabSubjects": "Subjects",
    "admin.thActions": "Actions",
    "admin.thLastActive": "Last active",
    "admin.thLeader": "Lead Lecturer",
    "admin.thName": "Name",
    "admin.thRole": "Role",
    "admin.thStatus": "Status",
    "admin.thSubject": "Subject",
    "admin.thSubjects": "Subjects",
    "admin.thUser": "User",
    "admin.unassignedSubject": "Unassigned",
    "admin.userListEyebrow": "Account list",
    "admin.userListTitle": "Users in system",
    "admin.usersAndAssignment": "Users & Assignment",
    "admin.usersDesc": "Manage accounts, roles, and subject assignments in one place.",
    "admin.usersUnit": "users",
    "admin.assignedClickToRemove": "Click to remove",
    "admin.assignedTooltip": "Assigned - cannot remove",
    "admin.emailPlaceholder": "example@domain.com",
    "admin.namePlaceholder": "Example: John Doe",
    "admin.optional": "Optional",
    "admin.passwordPlaceholder": "Minimum 8 characters",
    "admin.searchPlaceholder": "Name, email or subject...",
    "admin.subjectCodePlaceholder": "e.g., IOT102",
    "admin.tabsAria": "User and subject administration",
    "admin.unassignButton": "Unassign",
    "admin.delErrSelf": "Cannot delete your own account",
    "admin.delErrLastAdmin": "Cannot delete the last admin",
    "admin.delErrAssigned": "Cannot delete because user has assigned subjects",
    "admin.delErrActive": "User has been active recently",
    "admin.delOk": "Delete this user"
,
    "admin.activeStudents": "Active students",
    "admin.aiAnalyzeBtn": "Analyze and recommend",
    "admin.aiAssistantDesc": "Read metrics in the selected range and suggest priorities.",
    "admin.aiAssistantTitle": "Analytics Assistant",
    "admin.aiEmptyState": "Click the button to receive up to 4 recommendations based on questions, documents, users and payments.",
    "admin.chatQuestions": "Chat questions",
    "admin.days180": "Last 180 days",
    "admin.days30": "Last 30 days",
    "admin.days7": "Last 7 days",
    "admin.days90": "Last 90 days",
    "admin.docsIndexed": "indexed documents",
    "admin.docsReady": "Ready documents",
    "admin.genAi": "AI generated",
    "admin.genRules": "Rule-based",
    "admin.priHigh": "High priority",
    "admin.priLow": "Monitor",
    "admin.priNormal": "Should process",
    "admin.reset30": "Reset to 30 days",
    "admin.scopeAll": "All subjects · All users · Local timezone",
    "admin.scopeLabel": "Scope:",
    "admin.statTab.chatbot": "Chatbot",
    "admin.statTab.documents": "Documents",
    "admin.statTab.overview": "Overview",
    "admin.statTab.permissions": "Permissions",
    "admin.statTab.subscription": "Subscriptions",
    "admin.statTab.users": "Users",
    "admin.time": "Time period",
    "admin.updatedAt": "Updated",
    "stat.activeUsersUnit": "active users",
    "stat.answerRate": "Answer rate",
    "stat.answers": "answers",
    "stat.assistantResponsesUnit": "assistant responses",
    "stat.basedOnAnswers": "Based on assistant responses",
    "stat.dailyChatUsage": "Daily Chat Usage",
    "stat.dailyChatUsageDesc": "Track days with high student inquiry volume to supplement documents.",
    "stat.description": "Analyze chatbot activity, documents, users and permissions in the system.",
    "stat.docErrors": "Document errors",
    "stat.docsIndexFailed": "documents failed to index",
    "stat.docsReadyForChatbot": "Documents ready for chatbot",
    "stat.documentsReport": "Documents Report",
    "stat.documentsReportDesc": "Monitor upload volume, document processing status and identify subjects lacking learning materials.",
    "stat.indexFailed": "Index failed",
    "stat.lastDays": "recent days",
    "stat.mostCitedSubject": "Most cited subject",
    "stat.needMoreSources": "Needs more learning sources",
    "stat.needsAction": "Needs Action",
    "stat.needsActionDesc": "Signals that require admin attention first.",
    "stat.noChatData": "No chat data",
    "stat.noChatDataDesc": "No chat sessions found in the selected period.",
    "stat.permissionsReport": "Permissions Report",
    "stat.permissionsReportDesc": "Review learning material upload permissions and ensure all active subjects have an assigned lecturer.",
    "stat.questionsNeedChecking": "Questions needing review",
    "stat.quickInsight": "Quick Insight",
    "stat.studentsInSystem": "students in system",
    "stat.subjectsNoDocs": "Subjects without documents",
    "stat.subjectsNoLeader": "Subjects without leader",
    "stat.thereAre": "There are",
    "stat.title": "Reports & Statistics",
    "stat.topChatUsers": "Top Chat Users",
    "stat.topChatUsersDesc": "Identify students utilizing the learning assistant the most.",
    "stat.topSubjects": "Top Subjects",
    "stat.topSubjectsDesc": "List of subjects with the highest number of references by the chatbot.",
    "stat.unansweredQuestions": "Unanswered questions",
    "admin.reportScopeAria": "Select report data scope",
    "admin.timeSelectAria": "Select report time period",
    "admin.updatedAtTooltip": "Time when the system aggregated the report data"
  },
  vi: {

        "documents.status.indexed": "Đã Index",
        "documents.status.processing": "Đang xử lý",
        "documents.status.failed": "Thất bại",
        "documents.status.unknown": "Không rõ",
        "documents.failedTooltip": "Lỗi hệ thống khi xử lý tài liệu. Vui lòng kiểm tra lại file hoặc xoá đi tải lại.",

        "documents.sourceChoiceAria": "Chọn nguồn tài liệu",
        "documents.sourceFile": "Tải file",
        "documents.sourceUrl": "Nhập URL",
        "documents.indexFile": "Index file",
        "documents.indexUrl": "Index URL",
        "documents.indexing": "Đang index...",
        "chat.startSessionHint": "Bắt đầu phiên mới để trao đổi cùng trợ lý.",
        "shell.navLearning": "Học tập",
        "shell.contact": "Liên hệ",
        "account.showPassword": "Hiện mật khẩu",
        "account.hidePassword": "Ẩn mật khẩu",
        "account.provisioningInfo": "Tài khoản được cấp bởi Nhà trường. Vui lòng liên hệ Nhà trường để xin cấp tài khoản.",
        "chat.star": "Đánh dấu",
        "chat.unstar": "Bỏ đánh dấu",
        "chat.rename": "Đổi tên",
        "chat.delete": "Xóa",
        "confirm.title": "Xác nhận thao tác",
        "confirm.cancel": "Hủy",
        "confirm.accept": "Tiếp tục",
        "packages.learningPackage": "Gói học tập",
        "packages.viewAvailable": "Xem các gói đang bán",
        "packages.buyPackage": "Mua gói",
        "packages.free": "Miễn phí",
        "packages.discount": "Giảm",
        "packages.studentRecommended": "Khuyên dùng cho sinh viên",
        "packages.profileEffect": "Sở hữu hiệu ứng hồ sơ đặc biệt",
        "packages.prioritySupport": "Hỗ trợ ưu tiên, phản hồi nhanh hơn",
        "packages.cannotDowngrade": "Không thể hạ gói",
        "packages.lifetimeNoDowngrade": "Gói trọn đời không hỗ trợ hạ gói.",
        "packages.downgradeAfter": "Bạn có thể chọn gói thấp hơn sau",
        "packages.processing": "Đang xử lý...",
        "loading.page": "Đang tải nội dung...",
        "packages.chooseSuitable": "Chọn gói phù hợp",
        "packages.lifetimeActive": "Hiệu lực vĩnh viễn",
        "packages.remaining": "Còn",


        "chat.responsePaceAria": "Tốc độ hiển thị câu trả lời",
        "chat.paceFast": "Nhanh",
        "chat.paceComfortable": "Dễ theo dõi",
        "chat.replyMode": "Độ chi tiết",
        "chat.answerDepthAria": "Mức độ hỗ trợ",
        "chat.depthConcise": "Tóm tắt",
        "chat.depthBalanced": "Cân bằng",
        "chat.depthDeep": "Phân tích kỹ",
        "chat.remaining": "Còn lại",
        "chat.unlimited": "Không giới hạn",
        
        "packages.days": "ngày",
        "packages.lifetime": "Vĩnh viễn",
        
        "packages.name.free": "Free",
        "packages.desc.free": "Gói dùng thử cho sinh viên hỏi chatbot ở mức cơ bản.",
        "packages.name.student": "Student",
        "packages.desc.student": "Gói học tập hằng tháng cho sinh viên hỏi chatbot theo môn.",
        "packages.name.pro": "Pro",
        "packages.desc.pro": "Gói nâng cao cho sinh viên cần hỏi chatbot thường xuyên.",
        "packages.name.annual": "12 tháng",
        "packages.desc.annual": "Trọn một năm học với chi phí tiết kiệm hơn so với gia hạn từng tháng.",
        "packages.name.none": "Chưa có gói",


        "admin.shellTitleUsers": "Người dùng",
        "admin.shellTitleStats": "Báo cáo & Thống kê",
        "home.shellTitleChat": "Chatbot tài liệu",
        "home.shellTitleWorkspace": "Course workspace",
        "home.shellTitleCourses": "Môn học",
        "home.shellTitleDocs": "Kho tài liệu",
        "home.shellTitleOnlineUsers": "Online users",
        "home.shellTitleSubjectDocs": "Chi tiết môn học",
        "packages.shellTitle": "Gói dịch vụ",
        "payment.shellTitle": "Kết quả thanh toán",
        "subscriptions.shellTitle": "Tài khoản và thanh toán",


        "shell.title": "Course Assistant",
        "aiSettings.shellTitle": "Thiết lập trợ lý",


        "packages.startNow": "bắt đầu ngay",
        "packages.oneTimePayment": "thanh toán một lần",
        "packages.for": "cho",
        "packages.questionsPerMonth": "câu hỏi mỗi tháng",
        "packages.answerBasedOnDocs": "Trả lời dựa trên tài liệu môn học",
        "packages.saveHistory": "Lưu lịch sử và nguồn tham khảo",
        "packages.duration": "Thời hạn:",
        "packages.activateNow": "Kích hoạt ngay",
        "packages.selectPackage": "Chọn gói này",
        "packages.choosePayment": "Chọn phương thức thanh toán",
        "packages.payWithMomo": "Thanh toán qua ứng dụng MoMo",
        "packages.payWithBanking": "Quét mã QR bằng ứng dụng ngân hàng",
        
        "payment.checkInfo": "Cần kiểm tra lại thông tin",
        "payment.backToPackages": "Quay lại chọn gói",
        "payment.payVia": "Thanh toán qua",
        "payment.orderCode": "Mã đơn hàng",
        "payment.startChat": "Bắt đầu hỏi đáp",
        "payment.reselectMethod": "Chọn lại phương thức",
        "payment.checkCurrentPackage": "Kiểm tra gói hiện tại",
        
        "documents.orUrl": "Hoặc URL bài giảng",
        "documents.addDocToSubject": "Thêm tài liệu vào môn",
        "documents.documentsLower": "tài liệu",
        "documents.addDocBtn": "Thêm tài liệu",
        "documents.noDocsInChapter": "Chưa có tài liệu trong chương/mục này.",
        
        "chat.replyPrefsAria": "Tùy chọn trả lời",
        "chat.speed": "Tốc độ hiển thị",
        "chat.speedHelp": "Hiệu ứng trả chữ",
        "chat.depthHelp": "Điều chỉnh nội dung",
        "chat.accuracy": "Độ chính xác",
        "chat.accuracyDesc": "Khuyến nghị cho sinh viên, đảm bảo thông tin dựa hoàn toàn vào tài liệu, có nguồn trích dẫn rõ ràng.",
        
        "admin.confirmRemoveSubjectStudent": "Gỡ môn khỏi sinh viên?",
        "admin.confirmRegisterSubjectStudent": "Đăng ký môn đã chọn cho sinh viên?",
        "admin.registerSubjectStudentAria": "Đăng ký môn cho sinh viên",
        "admin.documents": "Tài liệu",
        
        "subscriptions.noActivePackage": "Chưa có gói hoạt động",
        "subscriptions.choosePackageDesc": "Chọn gói phù hợp để bắt đầu hỏi đáp theo tài liệu.",
        "subscriptions.viewPackagesBtn": "Xem các gói",
        "subscriptions.startDate": "Bắt đầu",
        "subscriptions.status": "Trạng thái",
        "subscriptions.clickToView": "Bấm để xem",
        "subscriptions.recentTransactions": "giao dịch gần nhất",
        "subscriptions.noTransactions": "Chưa có giao dịch",
        "subscriptions.receiptDesc": "Biên nhận sẽ xuất hiện tại đây sau khi bạn thanh toán hoặc kích hoạt gói.",
        "subscriptions.receiptTitle": "BIÊN NHẬN THANH TOÁN",
        "subscriptions.buyer": "Người mua",
        "subscriptions.transactionId": "Mã giao dịch",
        "subscriptions.amount": "Số tiền",
        "subscriptions.paymentDate": "Ngày thanh toán",
        "subscriptions.paymentMethod": "Phương thức",
        "subscriptions.purchasedPackage": "Gói mua",
        "subscriptions.printReceipt": "In biên nhận",


        "admin.userTableHeading": "Bảng người dùng / User table",
        "admin.assignedCannotRemove": "Đã assign - không thể gỡ",
        "admin.confirmDeleteUser": "Xóa người dùng này? Thao tác này không thể hoàn tác.",
        "admin.assignedClickRemove": "Đã assign - click X để gỡ",
        "admin.confirmRemoveSubject": "Gỡ môn khỏi giảng viên?",
        "admin.removeSubject": "Gỡ môn",
        "admin.confirmRegisterSubject": "Đăng ký môn đã chọn cho giảng viên?",
        "admin.registerSubjectAria": "Đăng ký môn cho giảng viên",
        "admin.overviewAria": "Chỉ số tổng quan",
        "admin.chatSessions": "Phiên chat",

        "documents.backToRepo": "Quay lại kho tài liệu",
        "documents.editInfoTitle": "Sửa thông tin tài liệu",
        "documents.fileType": "Loại file",
        "documents.fileSize": "Dung lượng",
        "documents.displayName": "Tên hiển thị",
        "documents.displayNameDesc": "Không đổi file vật lý; chỉ đổi tên hiển thị và citation về sau.",
        "documents.saveChanges": "Lưu thay đổi",
        "documents.order": "Thứ tự",
        
        "packages.title": "Chọn gói học tập phù hợp",
        "packages.desc": "Nâng cấp số lượt hỏi đáp để học hiệu quả hơn cùng tài liệu môn học.",
        "packages.viewCurrent": "Xem gói hiện tại",
        "packages.currentPackageAria": "Gói đang sử dụng",
        "packages.currentPackage": "Gói đang sử dụng",
        "packages.validUntil": "Có hiệu lực đến",
        "packages.activeBadge": "Đang hoạt động",
        "packages.inUseBadge": "Đang sử dụng",
        "packages.popularBadge": "Phổ biến",
        
        "payment.resultEyebrow": "Kết quả giao dịch",
        
        "shell.openAccountMenu": "Mở menu tài khoản",
        "shell.accountNav": "Tài khoản",
        "shell.loggedInAs": "Đang đăng nhập với tên",
        "shell.packages": "Gói dịch vụ",
        "shell.statistics": "Thống kê",
        
        "subscriptions.profileAndBilling": "Hồ sơ và thanh toán",
        "subscriptions.manageAccount": "Quản lý tài khoản của bạn",
        "subscriptions.manageDesc": "Cập nhật tên hiển thị, theo dõi gói học tập và xem lại biên nhận giao dịch.",
        "subscriptions.explorePackages": "Khám phá các gói",
        "subscriptions.buyerInfo": "Thông tin người mua",
        "subscriptions.buyerInfoDesc": "Tên này xuất hiện trên tài khoản và biên nhận.",
        "subscriptions.currentPackage": "Gói học tập hiện tại",


        "account.changePasswordTitle": "Đổi mật khẩu",
        "account.security": "Account security",
        "account.currentPassword": "Mật khẩu hiện tại",
        "account.newPassword": "Mật khẩu mới",
        "account.confirmNewPassword": "Nhập lại mật khẩu mới",
        
        "account.forgotPassword": "Quên mật khẩu",
        "account.recovery": "Account recovery",
        "account.forgotPasswordDesc": "Nhập email tài khoản để nhận liên kết đặt lại mật khẩu.",
        "account.sendLink": "Gửi liên kết",
        "account.rememberedPassword": "Đã nhớ mật khẩu?",
        "account.login": "Đăng nhập",
        
        "account.resetPassword": "Đặt lại mật khẩu",
        "account.passwordResetEyebrow": "Password reset",
        "account.resetPasswordDesc": "Tạo mật khẩu mới cho tài khoản của bạn.",
        "account.backToLogin": "Quay lại đăng nhập",
        
        "account.loginTitle": "Đăng nhập",
        "account.loginDesc": "Truy cập kho tài liệu, quản lý nội dung đã index và tiếp tục phiên hỏi đáp theo nguồn.",
        "account.password": "Mật khẩu",
        "account.forgotPasswordLink": "Quên mật khẩu?",
        "account.loginGoogle": "Đăng nhập với Google",
        "account.noAccountContact": "Chưa có tài khoản? Vui lòng liên hệ Nhà trường để xin cấp tài khoản.",
        
        "account.createAccountTitle": "Tạo tài khoản",
        "account.newAccountEyebrow": "Tài khoản mới",
        "account.createAccountDesc": "Tạo tài khoản nội bộ để quản lý tài liệu và sử dụng phiên hỏi đáp.",
        "account.fullName": "Họ tên",
        "account.confirmPassword": "Nhập lại mật khẩu",
        "account.alreadyHaveAccount": "Đã có tài khoản?",


        "account.profile": "Thông tin người dùng",
        "account.profileDesc": "Chỉnh sửa tên hiển thị",
        "account.packagesDesc": "Xem và chọn gói học tập",
        "account.history": "Lịch sử giao dịch",
        "account.historyDesc": "Xem và in biên nhận",
        "account.changePassword": "Đổi mật khẩu",
        "account.changePasswordDesc": "Bảo vệ tài khoản",
        "account.logout": "Đăng xuất",
        "admin.navAiSettings": "Thiết lập trợ lý",
        "aiSettings.title": "Thiết lập trợ lý học tập",
        "aiSettings.eyebrow": "Cách trợ lý đọc và trả lời",
        "aiSettings.desc": "Điều chỉnh chất lượng trả lời và cách chia tài liệu thành các phần dễ tìm kiếm.",
        "aiSettings.answerMethod": "Cách tạo câu trả lời",
        "aiSettings.answerMethodDesc": "Chọn bộ máy dùng để hiểu câu hỏi và viết câu trả lời.",
        "aiSettings.answerModel": "Bộ máy trả lời",
        "aiSettings.answerModelHelp": "Ví dụ: gemini-3.5-flash. Chỉ thay đổi khi nhà cung cấp xác nhận tên mới.",
        "aiSettings.readMethod": "Cách đọc tài liệu",
        "aiSettings.readMethodDesc": "Thiết lập này ảnh hưởng đến khả năng tìm đúng nội dung trong tài liệu.",
        "aiSettings.readModel": "Bộ máy đọc tài liệu",
        "aiSettings.readDetail": "Mức chi tiết khi ghi nhớ",
        "aiSettings.readDetailHelp": "768 phù hợp với hầu hết tài liệu môn học.",
        "aiSettings.chunkMethod": "Cách chia nội dung",
        "aiSettings.chunkMethodDesc": "Đoạn ngắn tìm nhanh hơn; đoạn dài giữ được nhiều ngữ cảnh hơn.",
        "aiSettings.chunkLength": "Độ dài mỗi đoạn",
        "aiSettings.chunkLengthHelp": "Khuyến nghị: 800–1.200 ký tự.",
        "aiSettings.chunkOverlap": "Phần nối giữa hai đoạn",
        "aiSettings.chunkOverlapHelp": "Khuyến nghị: 50–150 ký tự để không mất ý.",
        "aiSettings.reindexNote": "Lưu ý: tài liệu đã xử lý trước đây cần được xử lý lại để dùng thiết lập mới.",
        "aiSettings.save": "Lưu thiết lập",

    "nav.documents": "Tài liệu",
    "nav.chat": "Hỏi đáp",
    "nav.logout": "Đăng xuất",
    "nav.login": "Đăng nhập",
    "nav.register": "Tạo tài khoản",
    "nav.billing": "Gói dịch vụ",
    "nav.reports": "Báo cáo & Thống kê",
    "nav.reports.overview": "Tổng quan",
    "nav.reports.chatbot": "Chatbot",
    "nav.reports.documents": "Tài liệu",
    "nav.reports.users": "Người dùng",
    "nav.reports.permissions": "Phân quyền",
    "nav.reports.subscription": "Gói đăng ký",
    "nav.reports.export": "Xuất báo cáo",
    "stat.timeRange": "Khoảng ngày",
    "stat.7days": "7 ngày",
    "stat.30days": "30 ngày",
    "stat.90days": "90 ngày",
    "stat.custom": "Tùy chỉnh",
    "stat.exportReport": "Xuất báo cáo",
    "stat.chatQuestions": "Câu hỏi chat",
    "stat.chatSessions": "Phiên chat",
    "stat.activeStudents": "Sinh viên hoạt động",
    "stat.documents": "Tài liệu",
    "stat.totalDocsInRepo": "Tổng tài liệu trong kho",
    "stat.indexed": "Đã index:",
    "stat.percentTotalDocs": "% tổng tài liệu",
    "stat.basedOnAssistant": "Dựa trên câu trả lời assistant",
    "stat.totalQuestions": "Tổng câu hỏi",
    "stat.totalSessions": "Tổng phiên chat",
    "stat.lowFeedback": "Feedback thấp",
    "stat.noRatingData": "Chưa có dữ liệu đánh giá sao",
    "stat.responseTime": "Thời Thời gian phản hồi",
    "stat.noLatencyData": "Chưa lưu latency",
    "stat.chatbotFilters": "Bộ lọc chatbot",
    "stat.chatbotFiltersDesc": "Chuẩn bị UI lọc theo ngày, môn học, trạng thái và feedback.",
    "stat.noData": "Chưa có dữ liệu",
    "stat.usersReport": "Báo cáo người dùng",
    "stat.usersReportDesc": "Phân tích sự tăng trưởng người dùng và tỷ lệ active.",
    "stat.students": "Sinh viên",
    "stat.lecturers": "Giảng viên",
    "stat.admins": "Quản trị viên",
    "stat.subscriptionsReport": "Báo cáo đăng ký gói",
    "stat.subscriptionsReportDesc": "Phân tích doanh thu và các gói dịch vụ được sinh viên đăng ký.",
    "stat.revenue": "Doanh thu",
    "stat.vnd": "VNĐ",
    "stat.payments": "Thanh toán",
    "stat.successfulPayments": "Lượt thanh toán thành công",
    "stat.newSubscriptions": "Lượt đăng ký mới",
    "stat.pendingPayments": "Chờ thanh toán",
    "stat.revenueLast30Days": "Doanh thu 30 ngày qua",
    "stat.paidPackages": "Gói đã thanh toán",
    "stat.pendingPackages": "Gói chờ xử lý",
    "stat.canceledPackages": "Gói đã hủy",
    "stat.subscriptionAllocation": "Phân bổ gói đăng ký",
    "stat.thServicePackage": "Gói dịch vụ",
    "stat.thUsers": "Người dùng",
    "stat.thProportion": "Tỷ trọng",
    "stat.freePackage": "Gói Free",
    "stat.proPackage": "Gói Pro",
    "stat.proMaxPackage": "Gói Pro Max",
    "stat.thereAreCurrently": "Đang có",
    "stat.newSubsInFilter": "lượt đăng ký mới trong khoảng lọc.",
    "stat.pendingPaymentsAccountFor": "Chờ thanh toán chiếm",
    "stat.totalTransactions": "tổng số giao dịch.",
    "shell.portal": "Cổng học tập",
    "shell.chatbot": "Chatbot",
    "shell.courses": "Môn học",
    "shell.documents": "Kho tài liệu",
    "shell.users": "Người dùng",
    "shell.help": "Trợ giúp",
    "shell.search": "Tìm kiếm trong hệ thống...",
    "shell.notifications": "Thông báo",
    "shell.history": "Lịch sử",
    "shell.changePassword": "Đổi mật khẩu",
    "admin.totalUsers": "Tổng người dùng",
    "admin.usersShown": "hiển thị",
    "admin.admins": "QUẢN TRỊ VIÊN",
    "admin.fullSystemAccess": "Toàn quyền hệ thống",
    "admin.manageSubjects": "Quản lý môn được phân công",
    "admin.readChatAccess": "Quyền đọc và chat",
    "admin.subjects": "MÔN HỌC",
    "admin.subjectsAssigned": "đã phân công",
    "admin.directory": "Danh bạ",
    "admin.searchDirectory": "Tìm kiếm",
    "admin.searchDirectoryPlaceholder": "Tên, email, vai trò, môn học...",
    "admin.roleFilter": "Vai trò",
    "admin.filter": "Lọc",
    "admin.clear": "Xóa bộ lọc",
    "admin.thCreated": "NGÀY TẠO",
    "admin.allIndexedDocs": "Tất cả tài liệu đã index",
    "admin.subjectLeaderSuffix": " (Trưởng bộ môn)",
    "admin.leaderRole": "Trưởng bộ môn",
    "admin.roleStudent": "Học sinh",
    "admin.roleLecturer": "Giảng viên",
    "admin.roleAdmin": "Quản trị viên",
    "documents.backToCatalog": "Quay lại danh sách môn",
    "documents.defaultDescription": "Quản lý chương, file và link bài giảng của môn này.",
    "documents.documentsUnit": "tài liệu",
    "documents.chaptersUnit": "chương",
    "documents.editSubjectInfo": "Sửa thông tin môn",
    "documents.subjectCode": "Mã môn",
    "documents.subjectDesc": "Mô tả",
    "documents.deleteSubject": "Xóa môn",
    "documents.saveSubject": "Lưu môn",
    "documents.addChapter": "Thêm chương/mục",
    "documents.chapterTitle": "Tên chương/mục",
    "documents.chapterTitlePlaceholder": "VD: Chapter 1 / Muc 1.1",
    "documents.sortOrder": "Thứ tự",
    "documents.addChapterBtn": "Thêm chương",
    "documents.addDocument": "Thêm tài liệu",
    "documents.documentFile": "File tài liệu",
    "documents.orLectureUrl": "Hoặc URL bài giảng",
    "documents.indexDocumentBtn": "Index tài liệu",
    "documents.chaptersAndDocuments": "Chương và tài liệu",
    "documents.noChapters": "Môn này chưa có chương/mục.",
    "documents.noChaptersHint": "Thêm chương trước, sau đó upload tài liệu vào đúng mục.",
    "documents.updateBtn": "Cập nhật",
    "documents.deleteBtn": "Xóa",
    "documents.addFileTo": "Thêm file vào ",
    "documents.chapterLabel": "Chương/mục",
    "documents.emptyChapter": "Chưa có tài liệu trong chương/mục này.",
    "documents.previewBtn": "Xem",
    "documents.editBtn": "Sửa",
    "documents.reindexBtn": "Re-index",
    "documents.deleteDocBtn": "Xóa",
    "documents.indexedContent": "Nội dung đã index",
    "documents.previewTitle": "Tài liệu",
    "documents.loadingContent": "Đang tải nội dung đã index...",
    "documents.reindexConfirm": "Re-index tài liệu này?",
    "documents.deleteConfirm": "Xóa tài liệu này khỏi kho index? Thao tác này không thể hoàn tác.",
    "documents.deleteChapterConfirm": "Xóa chương/mục này? Thao tác này không thể hoàn tác.",
    "documents.deleteSubjectConfirm": "Xóa môn này và toàn bộ chương bên trong? Thao tác này không thể hoàn tác.",
    "documents.catalogEmptyDesc": "Danh sách các chương đã tạo",
    "documents.emptyCatalogTitle": "Chưa có danh mục môn học.",
    "documents.emptyCatalogHint": "Tạo môn học trước, sau đó thêm chương để upload tài liệu theo danh mục chuẩn.",
    "documents.createFirstChapterBtn": "Tạo chương đầu tiên",
    "documents.docInfo": "Thông tin tài liệu",
    "documents.chooseFileBtn": "Chọn file",
    "documents.enterUrl": "Nhập URL trang bài giảng",
    "admin.usersTitle": "Người dùng",
    "admin.navDirectory": "Bảng người dùng",
    "admin.navOnlineUsers": "Người dùng online",
    "admin.navLecturerTable": "Bảng giảng viên",
    "admin.navStudentTable": "Bảng học sinh",
    "admin.navImportExcel": "Nhập từ Excel",
    "admin.navSubjectManagement": "Quản lý môn học",
    "admin.thStudents": "SINH VIÊN",
    "admin.deactivate": "Ẩn",
    "admin.activate": "Mở",
    "courses.eyebrow": "Không gian học tập",
    "courses.description": "Mở không gian học tập để xem tài liệu, chương/mục và trao đổi về môn học.",
    "courses.emptyTitle": "Chưa có không gian môn học nào.",
    "courses.emptyHint": "Vui lòng tải lên và index tài liệu môn học trước, sau đó quay lại đây.",
    "courses.noDescription": "Chưa có mô tả.",
    "courses.unassigned": "Chưa phân công",
    "courses.chaptersUnit": "chương",
    "courses.eyebrowAdmin": "Quản trị học liệu",
    "courses.eyebrowTeacher": "Không gian giảng dạy",
    "courses.titleAdmin": "Tổng quan môn học",
    "courses.titleTeacher": "Môn học của tôi",
    "courses.descAdmin": "Theo dõi độ phủ học liệu và mở từng môn để kiểm tra chi tiết.",
    "courses.descTeacher": "Ưu tiên môn còn thiếu tài liệu, sau đó mở workspace để quản lý chương và kiểm tra chatbot.",
    "courses.btnAdmin": "Quản lý danh mục",
    "courses.btnTeacher": "Tải tài liệu",
    "courses.kpiCourses": "Môn hiển thị",
    "courses.kpiChaptersUnit": "chương",
    "courses.kpiReadiness": "Học liệu sẵn sàng",
    "courses.kpiIndexedUnit": "tài liệu đã index",
    "courses.kpiAttention": "Cần xử lý",
    "courses.kpiAllReady": "Tất cả môn đã sẵn sàng",
    "courses.kpiNeedsAttention": "Môn thiếu hoặc lỗi học liệu",
    "courses.statusMissing": "Thiếu tài liệu",
    "courses.statusReady": "Sẵn sàng",
    "courses.statusAttention": "Cần kiểm tra",
    "courses.indexedStatus": "tài liệu đã index",
    "courseWorkspace.backToCourses": "Quay lại danh sách",
    "courseWorkspace.defaultDescription": "Không gian môn học chứa tài liệu, các chương và chat môn học.",
    "courseWorkspace.chatCourse": "Chat môn này",
    "courseWorkspace.manageDocuments": "Quản lý tài liệu",
    "courseWorkspace.documents": "TÀI LIỆU",
    "courseWorkspace.chapters": "CHƯƠNG",
    "courseWorkspace.lecturer": "GIẢNG VIÊN",
    "courseWorkspace.noEmail": "Chưa có email",
    "courseWorkspace.courseMap": "Bản đồ môn học",
    "courseWorkspace.chaptersAndDocs": "Chương và tài liệu",
    "courseWorkspace.emptyChaptersTitle": "Chưa có chương nào được tải nội dung lên.",
    "courseWorkspace.emptyChaptersHint": "Tải tài liệu lên hoặc nhờ giảng viên bổ sung nội dung.",
    "courseWorkspace.docsUnit": "tài liệu",
    "courseWorkspace.chunksUnit": "chunks",
    "admin.defaultRole": "Vai trò mặc định",
    "admin.defaultLecturerSubjects": "Môn mặc định cho giảng viên",
    "admin.confirmImportUsers": "Tạo người dùng hàng loạt và gửi email chào mừng?",
    "admin.confirmRoleChange": "Đổi vai trò của {user} sang {role}?",
    "admin.roleChangeTitle": "Đổi vai trò sẽ hỏi xác nhận và tự lưu.",
    "onlineUsers.title": "Người dùng online",
    "onlineUsers.connecting": "Đang kết nối...",
    "onlineUsers.loading": "Đang tải người dùng online...",
    "onlineUsers.empty": "Hiện không có người dùng nào hoạt động.",
    "onlineUsers.activeNow": "Đang hoạt động",
    "onlineUsers.realtimeNote": "Trạng thái realtime",
    "onlineUsers.statusHelp": "Danh sách tự cập nhật khi người dùng vào hoặc rời hệ thống.",
    "onlineUsers.summaryOne": "1 người dùng online (đang hoạt động)",
    "onlineUsers.summaryMany": "{count} người dùng online (đang hoạt động)",
    "onlineUsers.youAreOnline": "Bạn đang online",
    "onlineUsers.online": "Online",
    "onlineUsers.tabs": "{count} tab",
    "onlineUsers.expand": "Mở danh sách người dùng online",
    "onlineUsers.collapse": "Thu gọn danh sách người dùng online",
    "documents.shellTitle": "Kho tài liệu",
    "documents.manageTitle": "Quản lý kho tài liệu",
    "documents.manageSubtitle": "Upload tài liệu học tập, trích xuất nội dung và index để chatbot trả lời có nguồn.",
    "documents.uploadKicker": "Upload",
    "documents.dropTitle": "Kéo thả tài liệu vào đây",
    "documents.chooseFile": "Chọn tệp hoặc kéo thả vào đây",
    "documents.urlRenderHint": "Trang SPA/React/Vue sẽ được render trước khi trích xuất DOM.",
    "documents.storage": "Lưu trữ",
    "documents.uploadedSize": "Dung lượng đã upload",
    "documents.totalUploaded": "Tổng đã upload",
    "documents.documentList": "Danh sách tài liệu",
    "documents.showing": "Hiển thị",
    "documents.fileName": "Tên file",
    "documents.status": "Trạng thái",
    "documents.uploadDate": "Ngày upload",
    "documents.indexedStatus": "Đã Index",
    "documents.emptyTitle": "Chưa có tài liệu.",
    "documents.emptyHint": "Upload giáo trình hoặc URL bài giảng để bắt đầu hỏi đáp theo nguồn.",
    "assistant.open": "Mở trang chat",
    "assistant.hidden": "Chào bạn, mình là chatbot hỗ trợ tìm kiếm thông tin nhanh hơn.",
    "documents.title": "Kho tài liệu môn học",
    "documents.subtitle": "Quản lý tài liệu đã upload, trích xuất nội dung và lập chỉ mục cho trang hỏi đáp riêng.",
    "documents.openChat": "Mở trang chat",
    "documents.statsAria": "Thống kê kho tài liệu",
    "documents.statsDocuments": "Tài liệu",
    "documents.statsIndexed": "Đã index",
    "documents.kpiIndexedSub": "Sẵn sàng chat",
    "documents.statsProcessing": "Đang xử lý",
    "documents.kpiProcessingSub": "Đang chờ xử lý",
    "documents.kpiFailedSub": "Cần kiểm tra",
    "documents.uploadTitle": "Upload tài liệu",
    "documents.uploadSubtitle": "PDF, DOCX, PPTX, XLSX, TXT, MD, CSV hoặc URL trang bài giảng sẽ được trích xuất, chunk và embed tự động.",
    "documents.subject": "Môn học",
    "documents.chapter": "Chương",
    "documents.source": "Nguồn",
    "documents.subjectPlaceholder": "VD: IOT102",
    "documents.chapterPlaceholder": "VD: Chương 1 hoặc Tuần 1",
    "documents.dropzoneTitle": "Kéo thả tài liệu hoặc bấm để chọn file",
    "documents.dropzoneDefault": "Hỗ trợ PDF, DOCX, PPTX, XLSX, TXT, MD, CSV",
    "documents.or": "hoặc",
    "documents.url": "URL trang bài giảng",
    "documents.urlPlaceholder": "https://example.com/bai-giang-react-vue",
    "documents.urlHint": "Trang SPA/React/Vue sẽ được render bằng Playwright trước khi trích xuất DOM.",
    "documents.submit": "Tải lên và index",
    "documents.indexedTitle": "Tài liệu đã lập chỉ mục",
    "documents.filesUnit": "file",
    "documents.empty": "Chưa có tài liệu nào. Hãy tải tài liệu môn học để bắt đầu hỏi đáp theo nguồn.",
    "documents.view": "Xem",
    "documents.navList": "Danh s\u00e1ch t\u00e0i li\u1ec7u",
    "documents.navUpload": "Upload & index",
    "documents.navCatalog": "Danh m\u1ee5c m\u00f4n h\u1ecdc",
    "documents.eyebrow": "Quản lý nội dung học tập",
    "documents.kpiTotal": "Tổng cộng",
    "documents.kpiFailed": "Thất bại",
    "documents.selectSubject": "Chọn môn học",
    "documents.noSubjectAssigned": "Chưa có môn nào được gán cho tài khoản này.",
    "documents.reindexStale": "Re-index tài liệu cũ",
    "documents.search": "Tìm kiếm",
    "documents.searchPlaceholder": "Tên file, môn học, chương...",
    "documents.allSubjects": "Tất cả môn học",
    "documents.allStatus": "Tất cả trạng thái",
    "documents.filter": "Lọc",
    "documents.clear": "Xóa bộ lọc",
    "documents.uploader": "Người tải lên",
    "documents.subjectChapter": "Môn học / Chương",
    "documents.chunksSize": "Số chunk / Kích thước",
    "documents.chunksUnit": "chunk",
    "documents.unknown": "Không rõ",
    "documents.actions": "Thao tác",
    "documents.edit": "Sửa",
    "documents.reindex": "Re-index",
    "documents.delete": "Xóa",
    "chat.sessionsAria": "Lịch sử phiên chat",
    "chat.documents": "Kho tài liệu",
    "chat.newSession": "Phiên mới",
    "chat.history": "Lịch sử phiên",
    "chat.sessionsUnit": "phiên",
    "chat.messagesUnit": "tin",
    "chat.noSessions": "Chưa có phiên nào.",
    "chat.mainAria": "Chat theo tài liệu",
    "chat.title": "Chat tài liệu",
    "chat.subtitle": "Hỏi theo kho tài liệu đã index. Câu hỏi ngoài phạm vi tài liệu sẽ được báo không đủ dữ liệu.",
    "chat.headerKicker": "Trợ lý môn học",
    "chat.headerTitle": "Nhập môn AI",
    "chat.headerSubtitle": "Hỏi đáp dựa trên tài liệu đã index. Nếu dữ liệu không đủ, chatbot phải báo thiếu nguồn thay vì đoán.",
    "chat.currentSession": "Phiên hiện tại",
    "chat.suggestionsAria": "Gợi ý câu hỏi",
    "chat.welcomeNamed": "Chào {name}! Mình là trợ lý học tập của bạn. Bạn muốn mình tra cứu, tóm tắt hay giải thích nội dung nào trong tài liệu hôm nay?",
    "chat.answerLabel": "Câu trả lời",
    "chat.followUpLabel": "Bạn có thể hỏi thêm",
    "chat.placeholder": "Hỏi về môn, chương hoặc tài liệu đã index...",
    "chat.send": "Gửi",
    "chat.relatedLabel": "Câu hỏi liên quan",
    "chat.relatedAria": "Câu hỏi liên quan",
    "chat.allSubjects": "Tất cả môn",
    "chat.defaultSessionTitle": "Phiên chưa có câu hỏi",
    "chat.sessionActions": "Thao tác phiên",
    "chat.starSession": "Ghim",
    "chat.unstarSession": "Bỏ ghim",
    "chat.renameSession": "Đổi tên",
    "chat.deleteSession": "Xóa",
    "chat.renamePrompt": "Tên phiên mới",
    "chat.deleteConfirm": "Xóa phiên chat này?",
    "chat.sessionActionError": "Không cập nhật được phiên chat.",
    "chat.loading": "Chờ mình chút, đang dò trong tài liệu...",
    "chat.requestError": "Không xử lý được câu hỏi.",
    "chat.connectionError": "Không kết nối được server. Kiểm tra lại ứng dụng rồi thử tiếp.",
    "chat.suggestions": [
      "Hiện có những môn nào đã index tài liệu?",
      "Tóm tắt các tài liệu đã upload.",
      "Tôi có thể hỏi gì từ kho tài liệu?",
      "Nên đọc tài liệu nào trước?"
    ],
    "admin.chatQuestionsNote": "Câu hỏi chat",
    "stat.noChatDataDesc2": "Sinh viên chưa sử dụng chatbot trong khoảng thời gian này. Thử đổi bộ lọc ngày hoặc kiểm tra dữ liệu chat.",
    "admin.dailyChartAria": "Biểu đồ sử dụng chat theo ngày",
    "admin.viewDetails": "Xem chi tiết",
    "admin.viewAll": "Xem tất cả",
    "admin.topSubjectsCaption": "Các môn học có nhiều lượt trích dẫn trong chatbot",
    "admin.noData": "Chưa có dữ liệu",
    "admin.noSubjectsQueried": "Không có môn học nào được hỏi trong khoảng thời gian này.",
    "stat.thSubject": "Môn học",
    "stat.thQuestions": "Câu hỏi",
    "stat.thDocs": "Tài liệu",
    "stat.thReadiness": "Tỷ lệ sẵn sàng",
    "admin.questionsUnit": "câu hỏi",
    "admin.totalQuestions": "Tổng câu hỏi",
    "admin.totalChatSessions": "Tổng phiên chat",
    "stat.unanswered": "Câu hỏi chưa trả lời",
    "stat.needMoreData": "Cần bổ sung dữ liệu",
    "admin.chatbotLimitsAria": "Giới hạn dữ liệu chatbot",
    "admin.currentMeasurementScope": "Phạm vi đo lường hiện tại",
    "admin.measurementScopeDesc": "Báo cáo đang tổng hợp số câu hỏi, phiên chat và người dùng hoạt động. Feedback và thời gian phản hồi chưa được thu thập nên không được hiển thị như KPI.",
    "admin.compactTableDesc": "Bảng gọn thay cho nhiều bảng dàn trải trên cùng một màn hình.",
    "admin.chatbotDailyUsageCaption": "Sử dụng chatbot theo từng ngày trong phạm vi báo cáo",
    "admin.thDate": "Ngày",
    "admin.thChatSessions": "Phiên chat",
    "admin.thActiveUsers": "Người dùng hoạt động",
    "admin.totalDocs": "Tổng tài liệu",
    "admin.inAllSystem": "Trong toàn hệ thống",
    "admin.indexed": "Đã index",
    "admin.processing": "Đang xử lý",
    "admin.processingDesc": "Đang chunk/index",
    "admin.checkFiles": "Cần kiểm tra file",
    "admin.subjectsNoDocs": "Môn chưa có tài liệu",
    "admin.needMoreDocs": "Cần bổ sung học liệu",
    "admin.recentDocsCaption": "Các tài liệu được tải lên gần đây",
    "admin.thDocument": "Tài liệu",
    "admin.thUploader": "Người upload",
    "admin.thCitation": "Citation",
    "admin.thUploadDate": "Ngày upload",
    "admin.unknown": "Chưa rõ",
    "admin.status.indexed": "Đã Index",
    "admin.status.processing": "Đang xử lý",
    "admin.status.failed": "Thất bại",
    "admin.students": "Sinh viên",
    "admin.chatbotUsageLevel": "Mức độ sử dụng chatbot.",
    "admin.totalStudents": "Tổng sinh viên",
    "admin.activeInDays": "Hoạt động trong {0} ngày",
    "admin.mostQuestions": "Hỏi nhiều nhất",
    "admin.lecturers": "Giảng viên",
    "admin.byUploadPerms": "Theo quyền upload và tài liệu.",
    "admin.totalLecturers": "Tổng giảng viên",
    "admin.activeUploadPerms": "Quyền upload đang hoạt động",
    "admin.avgDocsPerSubject": "TB tài liệu/môn",
    "admin.topChatUsersNote": "Admin biết nhóm sinh viên đang dùng chatbot tích cực.",
    "admin.topUsersCaption": "Những người dùng sử dụng chatbot nhiều nhất",
    "admin.thLastQuestion": "Lần hỏi gần nhất",
    "admin.leaderLecturer": "Giảng viên phụ trách",
    "admin.assignedSubjects": "Môn đã có người phụ trách",
    "admin.uploadPerms": "Quyền upload",
    "admin.assignedBySubject": "Đang gán theo môn",
    "admin.unassignedSubjects": "Môn chưa phụ trách",
    "admin.needsLeader": "Cần gán giảng viên phụ trách",
    "admin.assignLeaderBtn": "Gán giảng viên phụ trách",
    "admin.leaderStatusCaption": "Tình trạng phụ trách môn học của giảng viên",
    "admin.thLeaderSubject": "Môn phụ trách",
    "admin.thUploadPerms": "Quyền upload",
    "admin.unassigned": "Chưa phân công",
    "admin.yes": "Có",
    "admin.notYet": "Chưa có",
    "admin.status.needsAssign": "Cần gán",
    "admin.status.active": "Active",
    "admin.aiEyebrow": "Cách trợ lý đọc và trả lời",
    "admin.aiSettingsTitle": "Thiết lập trợ lý",
    "admin.aiSettingsDesc": "Chọn cách trợ lý đọc tài liệu và tạo câu trả lời cho người học.",
    "admin.genAnswerTitle": "Tạo câu trả lời",
    "admin.genAnswerDesc": "Chọn dịch vụ và bộ máy dùng để hiểu câu hỏi, sau đó viết câu trả lời.",
    "admin.aiService": "Dịch vụ AI",
    "admin.apiKeyProtected": "Khóa kết nối được bảo vệ trong cấu hình máy chủ, không hiển thị trên trang.",
    "admin.answerEngine": "Bộ máy trả lời",
    "admin.geminiDefaultGroqSpeed": "Gemini phù hợp mặc định; Groq là lựa chọn tốc độ cao cho sinh câu trả lời RAG.",
    "admin.provider.configured": "Dịch vụ đã kết nối",
    "admin.provider.missing": "Dịch vụ chưa có khóa kết nối",
    "admin.readDocTitle": "Đọc tài liệu",
    "admin.readDocDesc": "Chọn cách trợ lý ghi nhớ nội dung để tìm đúng đoạn liên quan khi có câu hỏi.",
    "admin.readEngine": "Bộ máy đọc tài liệu",
    "admin.detailLevel": "Mức độ chi tiết khi ghi nhớ",
    "admin.detailLevelDesc": "Mặc định 768. Mức cao hơn giữ nhiều chi tiết nhưng cần thêm dung lượng và thời gian tìm kiếm.",
    "admin.chunkDocTitle": "Chia tài liệu thành đoạn",
    "admin.chunkDocDesc": "Khi upload file lớn, hệ thống sẽ cắt nhỏ file ra thành nhiều đoạn. Đoạn ngắn giúp tìm kiếm chính xác, đoạn dài giữ được nhiều ngữ cảnh.",
    "admin.maxChunkLength": "Độ dài tối đa mỗi đoạn (Ký tự)",
    "admin.chunkLengthDesc": "Khuyến nghị: 800–1.200 ký tự.",
    "admin.chunkOverlap": "Phần nội dung lặp lại giữa hai đoạn",
    "admin.chunkOverlapDesc": "Giúp trợ lý không bỏ sót ý ở điểm cắt. Khuyến nghị khoảng 10% độ dài đoạn.",
    "admin.docSimulation": "Mô phỏng tài liệu...",
    "admin.chunk1": "Đoạn 1",
    "admin.chunk2": "Đoạn 2",
    "admin.overlapTooltip": "Phần giao nhau (Overlap)",
    "admin.note": "Lưu ý:",
    "admin.chunkWarning": "Khi bạn thay đổi thông số cắt tài liệu, các tài liệu đã được tải lên trước đây sẽ cần phải được xử lý (Embedding) lại để có tác dụng.",
    "admin.saveConfig": "Lưu cấu hình hệ thống",
    "admin.groqHint": "Khuyến nghị llama-3.3-70b-versatile cho chất lượng hoặc llama-3.1-8b-instant cho tốc độ.",
    "admin.geminiHint": "Khuyến nghị gemini-3.5-flash cho tốc độ và chi phí cân bằng.",
    "shell.assistantSettings": "Thiết lập trợ lý",
    "shell.reports": "Báo cáo & Thống kê",

    "admin.active": "Active",
    "admin.addAccount": "Thêm tài khoản",
    "admin.allRoles": "Tất cả vai trò",
    "admin.apply": "Áp dụng",
    "admin.assignButton": "Phân công",
    "admin.clearFilters": "Xóa bộ lọc",
    "admin.createAccountHeading": "Thêm user",
    "admin.createLecturerHint": "Giảng viên có quyền quản lý môn học và kho tài liệu.",
    "admin.createStudentHint": "Sinh viên có thể hỏi chatbot các môn được gán.",
    "admin.createSubjectPrompt": "Mã môn học",
    "admin.createUserBtn": "Tạo tài khoản",
    "admin.delete": "Xóa",
    "admin.description": "Mô tả",
    "admin.email": "Email",
    "admin.excelFile": "File Excel",
    "admin.excelImportHint": "Tải lên file .xlsx chứa danh sách tài khoản.",
    "admin.eyebrow": "Quản trị hệ thống",
    "admin.fullName": "Họ và tên",
    "admin.importExcelEyebrow": "Nhập hàng loạt",
    "admin.importExcelTitle": "Nhập từ Excel",
    "admin.importUsers": "Nhập tài khoản",
    "admin.importWelcomeNote": "Tải file mẫu và upload để tạo nhiều tài khoản cùng lúc.",
    "admin.inactive": "Ngưng hoạt động",
    "admin.kpiDeletable": "Có thể xóa",
    "admin.kpiDeletableDesc": "Không phụ trách môn và ngưng hoạt động trên 3 tháng",
    "admin.kpiLecturers": "Giảng viên",
    "admin.kpiLecturersDesc": "Quản lý môn được phân công",
    "admin.kpiStudents": "Sinh viên",
    "admin.kpiStudentsDesc": "Được sử dụng trợ lý học tập",
    "admin.kpiTotalUsers": "Tổng người dùng",
    "admin.lecturerManagement": "Quản lý giảng viên",
    "admin.lecturersUnit": "giảng viên",
    "admin.lecturerTableCaption": "Bảng phân công giảng viên",
    "admin.lecturerTableHeading": "Giảng viên & Phân công",
    "admin.navCreateAccount": "Tạo tài khoản",
    "admin.navCreateSubject": "Tạo môn học",
    "admin.noLecturersFound": "Không tìm thấy giảng viên.",
    "admin.noMatchingUsers": "Không tìm thấy người dùng.",
    "admin.noMatchingUsersHint": "Hãy thử thay đổi bộ lọc hoặc thêm tài khoản mới.",
    "admin.noneLeader": "Chưa có",
    "admin.noStudentsFound": "Không tìm thấy sinh viên.",
    "admin.noSubjects": "Chưa có môn học",
    "admin.noSubjectsAvailable": "Không có môn học",
    "admin.notApplicable": "Không áp dụng",
    "admin.noUnassignedSubjects": "Đã phân công hết các môn",
    "admin.overview": "Tổng quan",
    "admin.password": "Mật khẩu",
    "admin.role": "Vai trò",
    "admin.role.admin": "Quản trị viên",
    "admin.role.lecturer": "Giảng viên",
    "admin.role.student": "Sinh viên",
    "admin.search": "Tìm kiếm",
    "admin.selectSubjectAssign": "Chọn môn học để gán...",
    "admin.showing": "Đang hiển thị",
    "admin.studentAllowed": "Được dùng trợ lý học tập",
    "admin.studentManagement": "Quản lý sinh viên",
    "admin.studentsUnit": "sinh viên",
    "admin.studentTableCaption": "Bảng sinh viên",
    "admin.studentTableHeading": "Sinh viên",
    "admin.subjectCode": "Mã môn học",
    "admin.subjectList": "Danh sách môn học",
    "admin.subjectsForLecturer": "Môn học phụ trách",
    "admin.subjectsUnit": "môn học",
    "admin.subjectTableCaption": "Danh sách môn học",
    "admin.tabCreateAccount": "Thêm tài khoản",
    "admin.tabImportExcel": "Nhập Excel",
    "admin.tableCaption": "Danh sách người dùng theo bộ lọc hiện tại",
    "admin.tabLecturers": "Phân công giảng viên",
    "admin.tabList": "Danh sách",
    "admin.tabSubjects": "Môn học",
    "admin.thActions": "Thao tác",
    "admin.thLastActive": "Hoạt động gần nhất",
    "admin.thLeader": "Phụ trách",
    "admin.thName": "Tên",
    "admin.thRole": "Vai trò",
    "admin.thStatus": "Trạng thái",
    "admin.thSubject": "Môn học",
    "admin.thSubjects": "Môn học",
    "admin.thUser": "Người dùng",
    "admin.unassignedSubject": "Chưa đăng ký môn",
    "admin.userListEyebrow": "Danh sách tài khoản",
    "admin.userListTitle": "Người dùng trong hệ thống",
    "admin.usersAndAssignment": "Người dùng & phân công",
    "admin.usersDesc": "Quản lý tài khoản, vai trò và phân công môn học tại một nơi.",
    "admin.usersUnit": "người dùng",
    "admin.assignedClickToRemove": "Click để gỡ gán",
    "admin.assignedTooltip": "Đã assign - không thể gỡ",
    "admin.emailPlaceholder": "vidu@domain.com",
    "admin.namePlaceholder": "Ví dụ: Nguyễn Văn A",
    "admin.optional": "Không bắt buộc",
    "admin.passwordPlaceholder": "Tối thiểu 8 ký tự",
    "admin.searchPlaceholder": "Tên, email hoặc môn học...",
    "admin.subjectCodePlaceholder": "VD: IOT102",
    "admin.tabsAria": "Quản trị người dùng và môn học",
    "admin.unassignButton": "Gỡ",
    "admin.delErrSelf": "Không thể xoá chính mình",
    "admin.delErrLastAdmin": "Không thể xoá admin duy nhất",
    "admin.delErrAssigned": "Không thể xoá vì đang phụ trách môn",
    "admin.delErrActive": "Người dùng mới hoạt động gần đây",
    "admin.delOk": "Xóa người dùng này"
,
    "admin.activeStudents": "Sinh viên hoạt động",
    "admin.aiAnalyzeBtn": "Phân tích và đề xuất",
    "admin.aiAssistantDesc": "Đọc các chỉ số trong phạm vi đang chọn và đề xuất việc nên ưu tiên.",
    "admin.aiAssistantTitle": "Trợ lý phân tích",
    "admin.aiEmptyState": "Nhấn nút để nhận tối đa 4 đề xuất dựa trên câu hỏi, học liệu, người dùng và thanh toán.",
    "admin.chatQuestions": "Câu hỏi chat",
    "admin.days180": "180 ngày gần nhất",
    "admin.days30": "30 ngày gần nhất",
    "admin.days7": "7 ngày gần nhất",
    "admin.days90": "90 ngày gần nhất",
    "admin.docsIndexed": "tài liệu đã index",
    "admin.docsReady": "Học liệu sẵn sàng",
    "admin.genAi": "AI phân tích",
    "admin.genRules": "Quy tắc thống kê",
    "admin.priHigh": "Ưu tiên cao",
    "admin.priLow": "Theo dõi",
    "admin.priNormal": "Nên xử lý",
    "admin.reset30": "Đặt lại 30 ngày",
    "admin.scopeAll": "Tất cả môn học · Tất cả người dùng · Múi giờ địa phương",
    "admin.scopeLabel": "Phạm vi:",
    "admin.statTab.chatbot": "Chatbot",
    "admin.statTab.documents": "Tài liệu",
    "admin.statTab.overview": "Tổng quan",
    "admin.statTab.permissions": "Phân quyền",
    "admin.statTab.subscription": "Gói đăng ký",
    "admin.statTab.users": "Người dùng",
    "admin.time": "Thời gian",
    "admin.updatedAt": "Cập nhật",
    "stat.activeUsersUnit": "người dùng hoạt động",
    "stat.answerRate": "Tỷ lệ trả lời được",
    "stat.answers": "câu trả lời",
    "stat.assistantResponsesUnit": "câu trả lời của trợ lý",
    "stat.basedOnAnswers": "Dựa trên phản hồi trợ lý",
    "stat.dailyChatUsage": "Sử dụng chatbot hàng ngày",
    "stat.dailyChatUsageDesc": "Theo dõi những ngày sinh viên hỏi nhiều để có kế hoạch bổ sung học liệu.",
    "stat.description": "Phân tích hoạt động chatbot, tài liệu, người dùng và phân quyền trong hệ thống.",
    "stat.docErrors": "Tài liệu lỗi",
    "stat.docsIndexFailed": "tài liệu lỗi index",
    "stat.docsReadyForChatbot": "Tài liệu sẵn sàng cho chatbot",
    "stat.documentsReport": "Báo cáo tài liệu",
    "stat.documentsReportDesc": "Theo dõi lượng upload, tình trạng xử lý tài liệu và phát hiện các môn học đang thiếu học liệu.",
    "stat.indexFailed": "Lỗi index",
    "stat.lastDays": "ngày gần nhất",
    "stat.mostCitedSubject": "Môn được trích dẫn nhiều nhất",
    "stat.needMoreSources": "Cần thêm nguồn học liệu",
    "stat.needsAction": "Cần chú ý",
    "stat.needsActionDesc": "Các tín hiệu cần quản trị viên xử lý sớm.",
    "stat.noChatData": "Chưa có dữ liệu chat",
    "stat.noChatDataDesc": "Không có phiên hỏi đáp nào được ghi nhận trong thời gian này.",
    "stat.permissionsReport": "Báo cáo phân quyền",
    "stat.permissionsReportDesc": "Rà soát quyền upload học liệu và đảm bảo mọi môn học đang active đều có giảng viên phụ trách.",
    "stat.questionsNeedChecking": "Câu hỏi cần xem lại",
    "stat.quickInsight": "Đọc lướt",
    "stat.studentsInSystem": "sinh viên trong hệ thống",
    "stat.subjectsNoDocs": "Môn chưa có tài liệu",
    "stat.subjectsNoLeader": "Môn chưa có phụ trách",
    "stat.thereAre": "Có",
    "stat.title": "Báo cáo & Thống kê",
    "stat.topChatUsers": "Người dùng hỏi nhiều nhất",
    "stat.topChatUsersDesc": "Biết được những sinh viên đang tận dụng trợ lý học tập nhiều nhất.",
    "stat.topSubjects": "Môn học được hỏi nhiều",
    "stat.topSubjectsDesc": "Danh sách các môn học được trợ lý trích dẫn nhiều nhất để trả lời câu hỏi.",
    "stat.unansweredQuestions": "Câu hỏi chưa trả lời được",
    "admin.reportScopeAria": "Chọn phạm vi dữ liệu báo cáo",
    "admin.timeSelectAria": "Chọn khoảng thời gian báo cáo",
    "admin.updatedAtTooltip": "Thời điểm hệ thống tổng hợp dữ liệu báo cáo"
  }
};

const languageKey = "courseAssistantLanguage";
const chatPage = document.querySelector(".rbl-chat-page");
const documentsPage = document.querySelector(".ops-documents-page");
const chatForm = document.getElementById("chatForm");
const questionInput = document.getElementById("questionInput");
const chatMessages = document.getElementById("chatMessages");
const newSessionButton = document.getElementById("newSessionButton");
const chatSessionList = document.getElementById("chatSessionList");
const activeSessionTitle = document.getElementById("activeSessionTitle");
const documentDropzone = document.getElementById("documentDropzone");
const documentFileInput = document.getElementById("documentFileInput");
const documentFileName = document.getElementById("documentFileName");
const documentPreviewModal = document.getElementById("documentPreviewModal");
const documentPreviewTitle = document.getElementById("documentPreviewTitle");
const documentPreviewMeta = document.getElementById("documentPreviewMeta");
const documentPreviewBody = document.getElementById("documentPreviewBody");
const assistantLauncher = document.getElementById("chatbotHelper");
const assistantLauncherButton = document.getElementById("chatbotHelperButton");
const onlineUsersWidget = document.getElementById("onlineUsersWidget");
const onlineUsersList = document.getElementById("onlineUsersList");
const onlineUsersSummary = document.querySelector("[data-online-users-summary]");
const onlineUsersToggle = document.querySelector("[data-online-users-toggle]");
const onlineUsersWidgetStateKey = "courseAssistantOnlineUsersWidgetCollapsed";
let isSending = false;
let latestOnlineUsersSnapshot = null;
const subjectQuestionSubjects = readSubjectQuestionSubjects();
const relatedQuestionPool = readRelatedQuestionPool();

function getLanguage() {
  return localStorage.getItem(languageKey) === "en" ? "en" : "vi";
}

function t(key) {
  return translations[getLanguage()][key] || translations.en[key] || key;
}

function getChatUserName() {
  return chatPage?.dataset.chatUserName?.trim() || (getLanguage() === "vi" ? "bạn" : "there");
}

function getPersonalizedWelcome() {
  return t("chat.welcomeNamed").replace("{name}", getChatUserName());
}

function readJsonDataAttribute(element, key, fallback) {
  if (!element?.dataset?.[key]) {
    return fallback;
  }

  try {
    return JSON.parse(element.dataset[key]);
  } catch {
    return fallback;
  }
}

function buildDefaultSuggestionItems() {
  const enSuggestions = readJsonDataAttribute(chatPage, "chatSuggestionsEn", translations.en["chat.suggestions"]);
  const viSuggestions = readJsonDataAttribute(chatPage, "chatSuggestionsVi", translations.vi["chat.suggestions"]);
  const total = Math.max(enSuggestions.length, viSuggestions.length);

  return Array.from({ length: total }, (_, index) => ({
    id: `default-${index}`,
    en: enSuggestions[index] || viSuggestions[index] || "",
    vi: viSuggestions[index] || enSuggestions[index] || ""
  })).filter((item) => item.en || item.vi);
}

function getChatSuggestionItems() {
  const selectedSubject = getSelectedSubjectFilter();
  if (selectedSubject) {
    return buildSubjectQuestionItems(selectedSubject);
  }

  return dedupeQuestionItems([
    ...buildAllSubjectQuestionItems(),
    ...relatedQuestionPool,
    ...buildDefaultSuggestionItems()
  ]);
}

function getChatSuggestions(language = getLanguage()) {
  const asked = readAskedQuestions();
  const basePool = getChatSuggestionItems();
  return getAvailableQuestionItems(basePool, asked, getSelectedSubjectFilter())
    .slice(0, 6)
    .map((item) => language === "vi" ? item.vi : item.en)
    .filter(Boolean);
}

function readSubjectQuestionSubjects() {
  const fromPayload = readJsonDataAttribute(chatPage, "chatSubjectSuggestions", [])
    .map((item) => item?.subject || item?.vi || item?.en || "")
    .filter(Boolean);
  const fromChips = [...document.querySelectorAll(".chat-subject-chip")]
    .map((button) => button.dataset.subjectFilter || "")
    .filter(Boolean);

  return [...new Set([...fromPayload, ...fromChips].map((subject) => subject.trim()).filter(Boolean))];
}

function dedupeQuestionItems(items) {
  const seen = new Set();
  const result = [];
  for (const item of items) {
    if (!item?.en || !item?.vi) {
      continue;
    }

    const key = `${normalizeQuestionForMemory(item.en)}|${normalizeQuestionForMemory(item.vi)}`;
    if (seen.has(key)) {
      continue;
    }

    seen.add(key);
    result.push(item);
  }

  return result;
}

function getAvailableQuestionItems(basePool, asked, selectedSubject = "") {
  const normalizedBasePool = dedupeQuestionItems(basePool);
  const available = normalizedBasePool.filter((item) => !questionWasAsked(item, asked));
  if (available.length > 0) {
    return available;
  }

  const recoveryPool = selectedSubject
    ? buildRecoveryQuestionItems(selectedSubject)
    : subjectQuestionSubjects.flatMap((subject) => buildRecoveryQuestionItems(subject));
  const availableRecoveryItems = dedupeQuestionItems(recoveryPool)
    .filter((item) => !questionWasAsked(item, asked));
  if (availableRecoveryItems.length > 0) {
    return availableRecoveryItems;
  }

  // A long-running session may exhaust both pools. Recycle the primary pool so
  // suggestions remain useful instead of permanently hiding the entire strip.
  return normalizedBasePool;
}

function readRelatedQuestionPool() {
  const items = readJsonDataAttribute(chatPage, "chatRelatedQuestions", []);
  if (Array.isArray(items) && items.length > 0) {
    return items
      .filter((item) => item?.en && item?.vi)
      .map((item, index) => ({
        id: item.id || `subject-${index}`,
        subject: item.subject || "",
        en: item.en,
        vi: item.vi
      }));
  }

  return [
    {
      id: "available-subjects",
      subject: "",
      en: "Which subjects have indexed documents?",
      vi: "Hiện có những môn nào đã index tài liệu?"
    },
    {
      id: "summarize-documents",
      subject: "",
      en: "Summarize the uploaded documents.",
      vi: "Tóm tắt các tài liệu đã upload."
    },
    {
      id: "askable-content",
      subject: "",
      en: "What can I ask from the document repository?",
      vi: "Tôi có thể hỏi gì từ kho tài liệu?"
    }
  ];
}

async function ensureVietnameseFontReady() {
  if (!document.fonts?.load) {
    return;
  }

  await Promise.all([
    document.fonts.load('400 16px "Be Vietnam Pro"'),
    document.fonts.load('600 16px "Be Vietnam Pro"'),
    document.fonts.load('800 16px "Be Vietnam Pro"')
  ]);
}

async function setLanguage(language) {
  const nextLanguage = language === "vi" ? "vi" : "en";
  if (getLanguage() === nextLanguage) {
    return;
  }

  try {
    if (nextLanguage === "vi") {
      await ensureVietnameseFontReady();
    }

    localStorage.setItem(languageKey, nextLanguage);
    document.documentElement.classList.add("is-language-changing");
    applyLanguage();
  } finally {
    window.requestAnimationFrame(() => {
      window.requestAnimationFrame(() => {
        document.documentElement.classList.remove("is-language-changing");
      });
    });
  }
}

window.applyTranslations = applyLanguage;

function applyLanguage() {
  const language = getLanguage();
  document.documentElement.lang = language === "vi" ? "vi" : "en";

  document.querySelectorAll("[data-i18n]").forEach((element) => {
    element.textContent = translations[language][element.dataset.i18n] || translations.en[element.dataset.i18n] || element.textContent;
  });

  document.querySelectorAll("[data-i18n-placeholder]").forEach((element) => {
    element.placeholder = translations[language][element.dataset.i18nPlaceholder] || translations.en[element.dataset.i18nPlaceholder] || element.placeholder;
  });

  document.querySelectorAll("[data-i18n-aria-label]").forEach((element) => {
    element.setAttribute("aria-label", translations[language][element.dataset.i18nAriaLabel] || translations.en[element.dataset.i18nAriaLabel] || element.getAttribute("aria-label"));
  });

  document.querySelectorAll("[data-i18n-title]").forEach((element) => {
    element.setAttribute("title", translations[language][element.dataset.i18nTitle] || translations.en[element.dataset.i18nTitle] || element.getAttribute("title"));
  });

  document.querySelectorAll("[data-language-option]").forEach((button) => {
    const isActive = button.dataset.languageOption === language;
    button.classList.toggle("is-active", isActive);
    button.setAttribute("aria-pressed", String(isActive));
  });

  document.querySelectorAll("[data-language-field]").forEach((field) => {
    field.value = language;
  });

  document.querySelectorAll("[data-assistant-greeting]").forEach((element) => {
    const name = element.dataset.assistantName || "you";
    element.textContent = language === "vi"
      ? `Chào ${name}, mình sẽ hỗ trợ bạn tìm kiếm thông tin nhanh hơn.`
      : `Hi ${name}, I can help you find information faster.`;
  });

  document.querySelectorAll("[data-chat-welcome]").forEach((element) => {
    renderAssistantContent(element, getPersonalizedWelcome(), {
      includeAnswerLabel: false
    });
  });
  refreshChatFollowUpSuggestions();

  updateSuggestionButtons();
  updateRelatedQuestionButtons();
  updateDropzoneDefaultText();
  refreshChatTimeLabels();
  if (latestOnlineUsersSnapshot) {
    renderOnlineUsersSnapshot(latestOnlineUsersSnapshot);
  }
  applyOnlineUsersWidgetState();
  document.documentElement.classList.remove("i18n-pending");
  document.documentElement.classList.add("i18n-ready");
}

function updateSuggestionButtons() {
  const language = getLanguage();
  const suggestions = getChatSuggestions(language);
  document.querySelectorAll(".suggestion-chip").forEach((button, index) => {
    const question = suggestions[index];
    if (!question) {
      button.hidden = true;
      button.dataset.question = "";
      return;
    }

    button.hidden = false;
    button.textContent = question;
    button.dataset.question = question;
  });
}

function updateRelatedQuestionButtons() {
  renderRelatedQuestions();
}

function getSelectedSubjectFilter() {
  return document.querySelector(".chat-subject-chip.is-active")?.dataset.subjectFilter || "";
}

function setSelectedSubjectFilter(subject) {
  const normalizedSubject = subject || "";
  document.querySelectorAll(".chat-subject-chip").forEach((button) => {
    const isActive = (button.dataset.subjectFilter || "") === normalizedSubject;
    button.classList.toggle("is-active", isActive);
    button.setAttribute("aria-pressed", String(isActive));
  });
  updateSuggestionButtons();
  renderRelatedQuestions();
  refreshChatFollowUpSuggestions();
}

function bindSubjectFilterChips() {
  document.querySelectorAll(".chat-subject-chip").forEach((button) => {
    button.addEventListener("click", () => {
      setSelectedSubjectFilter(button.dataset.subjectFilter || "");
      questionInput?.focus();
    });
  });
}

function bindSubjectSuggestionButtons() {
  document.querySelectorAll("[data-subject-suggestion]").forEach((button) => {
    if (button.dataset.subjectSuggestionBound === "true") {
      return;
    }

    button.dataset.subjectSuggestionBound = "true";
    button.addEventListener("click", () => {
      setSelectedSubjectFilter(button.dataset.subjectSuggestion || "");
      questionInput?.focus();
    });
  });
}

function applyInitialSubjectFromUrl() {
  if (!chatPage) {
    return;
  }

  const subject = new URLSearchParams(window.location.search).get("subject") || "";
  if (!subject.trim()) {
    return;
  }

  const matchingChip = [...document.querySelectorAll(".chat-subject-chip")]
    .find((button) => (button.dataset.subjectFilter || "").toLowerCase() === subject.trim().toLowerCase());
  if (matchingChip) {
    setSelectedSubjectFilter(matchingChip.dataset.subjectFilter || "");
  }
}

function normalizeQuestionForMemory(question) {
  return (question || "")
    .normalize("NFD")
    .replace(/\p{M}/gu, "")
    .toLowerCase()
    .replace(/[^\p{L}\p{N}]+/gu, " ")
    .trim();
}

function getAskedQuestionKey() {
  return `ragChatAskedQuestions:${getSessionId()}`;
}

function readAskedQuestions() {
  try {
    return new Set(JSON.parse(localStorage.getItem(getAskedQuestionKey()) || "[]"));
  } catch {
    return new Set();
  }
}

function rememberAskedQuestion(question) {
  const normalized = normalizeQuestionForMemory(question);
  if (!normalized) {
    return;
  }

  const asked = readAskedQuestions();
  asked.add(normalized);
  localStorage.setItem(getAskedQuestionKey(), JSON.stringify([...asked].slice(-80)));
}

function collectVisibleUserQuestions() {
  document.querySelectorAll(".message.user .bubble").forEach((bubble) => {
    rememberAskedQuestion(bubble.textContent || "");
  });
}

function getRelatedRotationIndex() {
  return Number(sessionStorage.getItem(`ragChatRelatedRotation:${getSessionId()}`) || "0");
}

function advanceRelatedRotation() {
  const key = `ragChatRelatedRotation:${getSessionId()}`;
  sessionStorage.setItem(key, String(getRelatedRotationIndex() + 1));
}

function questionWasAsked(item, asked) {
  return asked.has(normalizeQuestionForMemory(item.en))
    || asked.has(normalizeQuestionForMemory(item.vi));
}

function normalizeSubjectForCompare(subject) {
  return normalizeQuestionForMemory(subject);
}

function extractCourseCode(value) {
  const match = String(value || "").match(/\b[A-Za-z]{2,}\d{2,}\b/);
  return match ? match[0].toUpperCase() : "";
}

function questionItemMatchesSubject(item, subject) {
  const selectedCode = extractCourseCode(subject);
  const itemCode = extractCourseCode(item?.subject || `${item?.en || ""} ${item?.vi || ""}`);
  if (selectedCode && itemCode) {
    return selectedCode === itemCode;
  }

  const normalizedSubject = normalizeSubjectForCompare(subject);
  const normalizedItemSubject = normalizeSubjectForCompare(item?.subject || "");
  return normalizedSubject
    && normalizedItemSubject
    && (normalizedItemSubject === normalizedSubject
      || normalizedItemSubject.includes(normalizedSubject)
      || normalizedSubject.includes(normalizedItemSubject));
}

function getKnownQuestionItemsForSubject(subject) {
  return relatedQuestionPool.filter((item) => questionItemMatchesSubject(item, subject));
}

function buildSubjectQuestionItems(subject) {
  const trimmedSubject = (subject || "").trim();
  if (!trimmedSubject) {
    return [];
  }

  const knownItems = getKnownQuestionItemsForSubject(trimmedSubject);
  if (knownItems.length > 0) {
    return knownItems;
  }

  return [
    {
      id: `${normalizeSubjectForCompare(trimmedSubject)}-credits`,
      en: `How many credits does ${trimmedSubject} have?`,
      vi: `${trimmedSubject} có bao nhiêu tín chỉ?`
    },
    {
      id: `${normalizeSubjectForCompare(trimmedSubject)}-about`,
      en: `What is ${trimmedSubject} about?`,
      vi: `${trimmedSubject} là môn gì?`
    },
    {
      id: `${normalizeSubjectForCompare(trimmedSubject)}-contents`,
      en: `What are the main contents of ${trimmedSubject}?`,
      vi: `Nội dung chính của ${trimmedSubject} gồm những gì?`
    },
    {
      id: `${normalizeSubjectForCompare(trimmedSubject)}-assessment`,
      en: `How is ${trimmedSubject} assessed?`,
      vi: `${trimmedSubject} được đánh giá như thế nào?`
    },
    {
      id: `${normalizeSubjectForCompare(trimmedSubject)}-outcomes`,
      en: `What learning outcomes does ${trimmedSubject} mention?`,
      vi: `${trimmedSubject} có chuẩn đầu ra nào?`
    },
    {
      id: `${normalizeSubjectForCompare(trimmedSubject)}-materials`,
      en: `What materials or resources are used in ${trimmedSubject}?`,
      vi: `${trimmedSubject} dùng tài liệu hoặc nguồn học nào?`
    },
    {
      id: `${normalizeSubjectForCompare(trimmedSubject)}-student-tasks`,
      en: `What does the syllabus say students need to do in ${trimmedSubject}?`,
      vi: `Sinh viên cần làm gì trong ${trimmedSubject}?`
    },
    {
      id: `${normalizeSubjectForCompare(trimmedSubject)}-exams`,
      en: `What exam or assessment percentages are listed for ${trimmedSubject}?`,
      vi: `${trimmedSubject} có tỷ lệ thi hoặc đánh giá nào?`
    },
    {
      id: `${normalizeSubjectForCompare(trimmedSubject)}-chapters`,
      en: `Which chapters or sections are indexed for ${trimmedSubject}?`,
      vi: `${trimmedSubject} đã index những chương hoặc phần nào?`
    },
    {
      id: `${normalizeSubjectForCompare(trimmedSubject)}-objectives`,
      en: `What are the objectives of ${trimmedSubject}?`,
      vi: `Mục tiêu của ${trimmedSubject} là gì?`
    },
    {
      id: `${normalizeSubjectForCompare(trimmedSubject)}-prerequisites`,
      en: `Does ${trimmedSubject} mention any prerequisites?`,
      vi: `${trimmedSubject} có yêu cầu tiên quyết nào không?`
    },
    {
      id: `${normalizeSubjectForCompare(trimmedSubject)}-schedule`,
      en: `What study schedule or weekly plan is listed for ${trimmedSubject}?`,
      vi: `${trimmedSubject} có lịch học hoặc kế hoạch tuần nào?`
    },
    {
      id: `${normalizeSubjectForCompare(trimmedSubject)}-activities`,
      en: `What learning activities are mentioned in ${trimmedSubject}?`,
      vi: `${trimmedSubject} có những hoạt động học tập nào?`
    },
    {
      id: `${normalizeSubjectForCompare(trimmedSubject)}-tools`,
      en: `What tools or platforms are used in ${trimmedSubject}?`,
      vi: `${trimmedSubject} sử dụng công cụ hoặc nền tảng nào?`
    },
    {
      id: `${normalizeSubjectForCompare(trimmedSubject)}-completion`,
      en: `What completion criteria are listed for ${trimmedSubject}?`,
      vi: `${trimmedSubject} có tiêu chí hoàn thành nào?`
    },
    {
      id: `${normalizeSubjectForCompare(trimmedSubject)}-summary`,
      en: `Summarize the indexed syllabus for ${trimmedSubject}.`,
      vi: `Tóm tắt syllabus đã index của ${trimmedSubject}.`
    },
    {
      id: `${normalizeSubjectForCompare(trimmedSubject)}-important-notes`,
      en: `What important notes should students remember for ${trimmedSubject}?`,
      vi: `Sinh viên cần lưu ý gì khi học ${trimmedSubject}?`
    }
  ];
}

function buildAllSubjectQuestionItems() {
  return subjectQuestionSubjects.flatMap((subject) => buildSubjectQuestionItems(subject));
}

function buildRecoveryQuestionItems(subject) {
  const trimmedSubject = (subject || "").trim();
  if (!trimmedSubject) {
    return [];
  }

  const subjectKey = normalizeSubjectForCompare(trimmedSubject);
  return [
    {
      id: `${subjectKey}-recovery-teacher-expectations`,
      en: `What does the lecturer expect students to prepare for ${trimmedSubject}?`,
      vi: `Giảng viên yêu cầu sinh viên chuẩn bị gì cho ${trimmedSubject}?`
    },
    {
      id: `${subjectKey}-recovery-output-products`,
      en: `What products, assignments, or submissions are required in ${trimmedSubject}?`,
      vi: `${trimmedSubject} yêu cầu bài tập, sản phẩm hoặc bài nộp nào?`
    },
    {
      id: `${subjectKey}-recovery-study-resources`,
      en: `Which links, files, or learning resources are mentioned for ${trimmedSubject}?`,
      vi: `${trimmedSubject} có link, file hoặc nguồn học nào được nhắc đến?`
    },
    {
      id: `${subjectKey}-recovery-grading-guide`,
      en: `What grading guide or rubrics are mentioned for ${trimmedSubject}?`,
      vi: `${trimmedSubject} có hướng dẫn chấm điểm hoặc rubric nào?`
    },
    {
      id: `${subjectKey}-recovery-first-read`,
      en: `What should I read first in the indexed material for ${trimmedSubject}?`,
      vi: `Nên đọc phần nào trước trong tài liệu đã index của ${trimmedSubject}?`
    }
  ];
}

function updateRelatedQuestionsLabel(selectedSubject) {
  const label = document.querySelector(".chat-related-strip > span");
  if (!label) {
    return;
  }

  label.textContent = selectedSubject
    ? t("chat.relatedLabel")
    : (getLanguage() === "vi" ? "Câu hỏi gợi ý" : "Suggested questions");
}

function renderRelatedQuestions() {
  const list = document.querySelector(".chat-related-list");
  if (!list) {
    return;
  }

  const strip = list.closest(".chat-related-strip");
  collectVisibleUserQuestions();
  const language = getLanguage();
  const selectedSubject = getSelectedSubjectFilter();
  updateRelatedQuestionsLabel(selectedSubject);

  const asked = readAskedQuestions();
  const basePool = selectedSubject
    ? buildSubjectQuestionItems(selectedSubject)
    : dedupeQuestionItems([
        ...buildAllSubjectQuestionItems(),
        ...relatedQuestionPool,
        ...buildDefaultSuggestionItems()
      ]);
  const pool = getAvailableQuestionItems(basePool, asked, selectedSubject);
  if (pool.length === 0) {
    list.innerHTML = "";
    if (strip) {
      strip.hidden = true;
    }
    return;
  }

  if (strip) {
    strip.hidden = false;
  }
  const offset = getRelatedRotationIndex() % pool.length;
  const ordered = [...pool.slice(offset), ...pool.slice(0, offset)];
  const currentQuestions = new Set(
    [...list.querySelectorAll(".related-question-chip")]
      .map((button) => normalizeQuestionForMemory(button.dataset.question || button.textContent))
      .filter(Boolean));
  const picked = [];

  for (const item of ordered) {
    const text = language === "vi" ? item.vi : item.en;
    const normalized = normalizeQuestionForMemory(text);
    if (!normalized || picked.some((pickedItem) => pickedItem.id === item.id)) {
      continue;
    }

    if (pool.length > 8 && currentQuestions.has(normalized)) {
      continue;
    }

    picked.push(item);
    if (picked.length === 8) {
      break;
    }
  }

  if (picked.length < 8) {
    for (const item of ordered) {
      if (!picked.some((pickedItem) => pickedItem.id === item.id)) {
        picked.push(item);
      }
      if (picked.length === 8) {
        break;
      }
    }
  }

  list.innerHTML = picked.map((item) => {
    const text = language === "vi" ? item.vi : item.en;
    return `<button type="button" class="related-question-chip" data-question-id="${escapeHtml(item.id)}" data-question-subject="${escapeHtml(item.subject || "")}" data-question="${escapeHtml(text)}" data-question-en="${escapeHtml(item.en)}" data-question-vi="${escapeHtml(item.vi)}">${escapeHtml(text)}</button>`;
  }).join("");
  bindSuggestionButtons();
}

function updateDropzoneDefaultText() {
  if (!documentFileInput || !documentFileName || documentFileInput.files.length > 0) {
    return;
  }

  documentFileName.textContent = t("documents.dropzoneDefault");
}

function createSessionId() {
  if (crypto.randomUUID) {
    return crypto.randomUUID();
  }

  return "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g, (character) => {
    const random = Math.random() * 16 | 0;
    const value = character === "x" ? random : (random & 0x3 | 0x8);
    return value.toString(16);
  });
}

function getSessionId() {
  let sessionId = localStorage.getItem("ragChatSessionId");
  if (!sessionId) {
    sessionId = createSessionId();
    localStorage.setItem("ragChatSessionId", sessionId);
  }

  return sessionId;
}

function setSessionId(sessionId) {
  localStorage.setItem("ragChatSessionId", sessionId);
  markActiveSession(sessionId);
  updateSuggestionButtons();
  renderRelatedQuestions();
}

function formatSessionTime(value) {
  if (!value) {
    return "";
  }

  return new Date(value).toLocaleString(getLanguage() === "vi" ? "vi-VN" : "en-US", {
    day: "2-digit",
    month: "2-digit",
    hour: "2-digit",
    minute: "2-digit"
  });
}

function getSessionTitle(session) {
  return session?.title || t("chat.defaultSessionTitle");
}

function renderWelcomeMessage() {
  if (!chatMessages) {
    return;
  }

  chatMessages.replaceChildren();
  const welcomeMessage = appendMessageTo(chatMessages, "assistant", getPersonalizedWelcome(), [], {
    includeAnswerLabel: false
  });
  welcomeMessage?.querySelector(".bubble")?.setAttribute("data-chat-welcome", "");
  applyLanguage();
}

function renderSessionMessages(messages) {
  if (!chatMessages) {
    return;
  }

  chatMessages.innerHTML = "";
  if (!messages || messages.length === 0) {
    renderWelcomeMessage();
    return;
  }

  messages.forEach((message) => {
    appendMessageTo(chatMessages, message.role, message.content, message.citations || [], {
      createdAt: message.createdAt
    });
  });
  renderRelatedQuestions();
}

function markActiveSession(sessionId) {
  document.querySelectorAll(".chat-session-item").forEach((button) => {
    button.classList.toggle("is-active", button.dataset.sessionId === sessionId);
  });
}

function closeSessionMenus(exceptMenu = null) {
  document.querySelectorAll(".chat-session-menu.is-open").forEach((menu) => {
    if (menu !== exceptMenu) {
      menu.classList.remove("is-open");
    }
  });
}

function renderSessionList(sessions) {
  if (!chatSessionList) {
    return;
  }

  const sessionCount = document.getElementById("sessionCount");
  if (sessionCount) {
    sessionCount.textContent = sessions?.length ?? 0;
  }

  if (!sessions || sessions.length === 0) {
    chatSessionList.innerHTML = `
      <div class="session-empty">
        <strong data-i18n="chat.noSessions">${escapeHtml(t("chat.noSessions"))}</strong>
        <span data-i18n="chat.startSessionHint">${escapeHtml(t("chat.startSessionHint"))}</span>
      </div>`;
    applyLanguage();
    return;
  }

  chatSessionList.innerHTML = sessions.map((session) => `
    <div class="chat-session-entry${session.isStarred ? " is-starred" : ""}" data-session-entry data-session-id="${session.id}">
      <button type="button" class="chat-session-item" data-session-id="${session.id}">
        <span>${session.isStarred ? `<span class="material-symbols-outlined session-star" aria-hidden="true">star</span>` : ""}${escapeHtml(getSessionTitle(session))}</span>
        <small>${formatSessionTime(session.updatedAt)} / ${session.messageCount ?? 0} ${escapeHtml(t("chat.messagesUnit"))}</small>
      </button>
      <button type="button" class="chat-session-menu-button" data-session-menu-toggle data-session-id="${session.id}" aria-label="${escapeHtml(t("chat.sessionActions"))}">
        <span class="material-symbols-outlined" aria-hidden="true">more_vert</span>
      </button>
      <div class="chat-session-menu" data-session-menu>
        <button type="button" data-session-action="star" data-session-id="${session.id}" data-session-starred="${session.isStarred ? "true" : "false"}">
          <span class="material-symbols-outlined" aria-hidden="true">star</span>
          <span>${escapeHtml(t(session.isStarred ? "chat.unstarSession" : "chat.starSession"))}</span>
        </button>
        <button type="button" data-session-action="rename" data-session-id="${session.id}">
          <span class="material-symbols-outlined" aria-hidden="true">edit</span>
          <span>${escapeHtml(t("chat.renameSession"))}</span>
        </button>
        <button type="button" class="danger" data-session-action="delete" data-session-id="${session.id}">
          <span class="material-symbols-outlined" aria-hidden="true">delete</span>
          <span>${escapeHtml(t("chat.deleteSession"))}</span>
        </button>
      </div>
    </div>
  `).join("");
  bindSessionButtons();
  markActiveSession(getSessionId());
}

function escapeHtml(value) {
  const div = document.createElement("div");
  div.textContent = value || "";
  return div.innerHTML;
}


function setOnlineUsersConnectionState(state) {
  if (!onlineUsersWidget) {
    return;
  }

  onlineUsersWidget.dataset.connectionState = state || "disconnected";
}

function normalizeOnlineUsersPayload(payload) {
  if (!payload || typeof payload !== "object") {
    return null;
  }

  const users = Array.isArray(payload.users)
    ? payload.users.map((item) => ({
        userKey: item.userKey || "",
        userId: item.userId || "",
        displayName: item.displayName || item.email || "Unknown user",
        email: item.email || "",
        role: item.role || "",
        initials: item.initials || "U",
        isPremium: item.isPremium === true,
        connectionCount: Number(item.connectionCount || 0)
      }))
    : [];

  return {
    onlineUserCount: Number(payload.onlineUserCount || users.length || 0),
    users,
    windowLabel: payload.windowLabel || "active now"
  };
}

function formatOnlineUsersSummary(count) {
  const key = count === 1 ? "onlineUsers.summaryOne" : "onlineUsers.summaryMany";
  return t(key).replace("{count}", String(count));
}

function renderOnlineUsersSnapshot(snapshot) {
  if (!onlineUsersWidget || !onlineUsersList) {
    return;
  }

  if (!snapshot) {
    return;
  }

  const currentUserId = onlineUsersWidget.dataset.currentUserId || "";
  const currentUserEmail = (onlineUsersWidget.dataset.currentUserEmail || "").trim().toUpperCase();
  const users = [...snapshot.users].sort((left, right) => {
    const leftIsCurrent = (currentUserId && left.userId === currentUserId) || (currentUserEmail && left.email.trim().toUpperCase() === currentUserEmail);
    const rightIsCurrent = (currentUserId && right.userId === currentUserId) || (currentUserEmail && right.email.trim().toUpperCase() === currentUserEmail);
    if (leftIsCurrent !== rightIsCurrent) {
      return leftIsCurrent ? -1 : 1;
    }

    return left.displayName.localeCompare(right.displayName, undefined, { sensitivity: "base" });
  });

  if (onlineUsersSummary) {
    onlineUsersSummary.textContent = formatOnlineUsersSummary(snapshot.onlineUserCount);
  }

  if (users.length === 0) {
    onlineUsersList.innerHTML = "<li class=\"online-users-widget__empty\">" + escapeHtml(t("onlineUsers.empty")) + "</li>";
    return;
  }

  onlineUsersList.innerHTML = users.map((user) => {
    const isCurrentUser = (currentUserId && user.userId === currentUserId) || (currentUserEmail && user.email.trim().toUpperCase() === currentUserEmail);
    const premiumClass = user.isPremium ? " has-premium-effect" : "";

    return [
      "<li class=\"online-users-widget__item" + (isCurrentUser ? " is-current-user" : "") + (user.isPremium ? " is-premium-user" : "") + "\">",
      "  <span class=\"online-users-widget__avatar" + premiumClass + "\">" + escapeHtml(user.initials) + "</span>",
      "  <span class=\"online-users-widget__identity\">",
      "    <strong class=\"" + (user.isPremium ? "premium-user-name" : "") + "\">" + escapeHtml(user.displayName) + "</strong>",
      "  </span>",
      "</li>"
    ].join("");
  }).join("");
}

function applyOnlineUsersChanged(payload) {
  const snapshot = normalizeOnlineUsersPayload(payload);
  if (!snapshot) {
    return;
  }

  latestOnlineUsersSnapshot = snapshot;
  renderOnlineUsersSnapshot(snapshot);
}
function formatPreviewBytes(bytes) {
  const units = ["B", "KB", "MB", "GB", "TB"];
  let value = Math.max(0, Number(bytes) || 0);
  let unitIndex = 0;
  while (value >= 1024 && unitIndex < units.length - 1) {
    value /= 1024;
    unitIndex += 1;
  }

  return unitIndex === 0 ? `${value} ${units[unitIndex]}` : `${value.toFixed(value >= 10 ? 1 : 2).replace(/\.0+$/, "")} ${units[unitIndex]}`;
}

function formatPreviewDate(value) {
  if (!value) {
    return "";
  }

  return new Date(value).toLocaleString(getLanguage() === "vi" ? "vi-VN" : "en-US", {
    year: "numeric",
    month: "2-digit",
    day: "2-digit",
    hour: "2-digit",
    minute: "2-digit"
  });
}

function closeDocumentPreview() {
  if (!documentPreviewModal) {
    return;
  }

  documentPreviewModal.classList.remove("is-open");
  documentPreviewModal.setAttribute("aria-hidden", "true");
}

function renderDocumentPreview(document) {
  if (!documentPreviewTitle || !documentPreviewMeta || !documentPreviewBody) {
    return;
  }

  const chunks = Array.isArray(document.chunks) ? document.chunks : [];
  const embeddingLabel = document.embeddingModel
    ? `Embedding: ${document.embeddingModel}${document.embeddingDimensions ? ` (${document.embeddingDimensions} dims)` : ""}`
    : "";
  const indexedLabel = document.indexedAt ? `Indexed: ${formatPreviewDate(document.indexedAt)}` : "";
  const uploader = document.uploadedByName ? `Upload: ${document.uploadedByName}` : "Upload: Không rõ";
  const subjectOwner = document.subjectOwnerName ? `Phụ trách: ${document.subjectOwnerName}` : "Phụ trách: Chưa phân công";
  documentPreviewTitle.textContent = document.fileName || "Tài liệu";
  documentPreviewMeta.textContent = [
    document.subject,
    document.chapter,
    document.status ? `Status: ${document.status}` : "",
    indexedLabel,
    uploader,
    subjectOwner,
    embeddingLabel,
    document.chunkingStrategy ? `Chunking: ${document.chunkingStrategy}` : "",
    `${chunks.length || document.chunkCount || 0} chunks`,
    formatPreviewBytes(document.fileSizeBytes),
    formatPreviewDate(document.uploadedAt)
  ].filter(Boolean).join(" / ");

  if (chunks.length === 0) {
    documentPreviewBody.innerHTML = `
      <div class="rbl-empty-state compact">
        <strong>Chưa có nội dung index.</strong>
        <p>Tài liệu này chưa có chunk text để hiển thị.</p>
      </div>`;
    return;
  }

  const totalChars = chunks.reduce((sum, chunk) => sum + (chunk.text || "").length, 0);
  const summary = `
    <div class="rbl-index-summary">
      <article><span>Chunks</span><strong>${escapeHtml(String(chunks.length))}</strong></article>
      <article><span>Indexed chars</span><strong>${escapeHtml(String(totalChars))}</strong></article>
      <article><span>Strategy</span><strong>${escapeHtml(document.chunkingStrategy || "unknown")}</strong></article>
    </div>`;

  documentPreviewBody.innerHTML = summary + chunks.map((chunk) => `
    <article class="rbl-document-preview-chunk">
      <span>Chunk ${escapeHtml(String(chunk.chunkIndex ?? ""))}${chunk.sectionTitle ? ` / ${escapeHtml(chunk.sectionTitle)}` : ""}</span>
      <small>${[
        Number.isInteger(chunk.charStart) && Number.isInteger(chunk.charEnd) ? `${chunk.charStart}-${chunk.charEnd}` : "",
        chunk.text ? `${chunk.text.length} chars` : ""
      ].filter(Boolean).map((value) => escapeHtml(String(value))).join(" / ")}</small>
      <p>${escapeHtml(chunk.text || "")}</p>
    </article>
  `).join("");
}

async function openDocumentPreview(url) {
  if (!documentPreviewModal || !documentPreviewBody) {
    return;
  }

  documentPreviewModal.classList.add("is-open");
  documentPreviewModal.setAttribute("aria-hidden", "false");
  if (documentPreviewTitle) {
    documentPreviewTitle.textContent = "Tài liệu";
  }
  if (documentPreviewMeta) {
    documentPreviewMeta.textContent = "";
  }
  documentPreviewBody.innerHTML = `<p class="rbl-catalog-muted">Đang tải nội dung đã index...</p>`;

  try {
    const response = await fetch(url, { headers: { "Accept": "application/json" } });
    const payload = await response.json();
    if (!response.ok) {
      throw new Error(payload.error || "Could not load document preview.");
    }

    renderDocumentPreview(payload);
  } catch (error) {
    documentPreviewBody.innerHTML = `
      <div class="rbl-alert is-error">
        ${escapeHtml(error.message || "Không tải được nội dung tài liệu.")}
      </div>`;
  }
}

function bindDocumentPreviewButtons() {
  document.querySelectorAll("[data-document-preview-action], [data-document-preview-url]").forEach((button) => {
    if (button.dataset.documentPreviewBound === "true") {
      return;
    }

    button.dataset.documentPreviewBound = "true";
    button.addEventListener("click", () => {
      if (button.disabled || !button.dataset.documentPreviewUrl) {
        return;
      }

      openDocumentPreview(button.dataset.documentPreviewUrl);
    });
  });

  document.querySelectorAll("[data-document-preview-close]").forEach((button) => {
    if (button.dataset.documentPreviewCloseBound === "true") {
      return;
    }

    button.dataset.documentPreviewCloseBound = "true";
    button.addEventListener("click", closeDocumentPreview);
  });

  if (document.body.dataset.documentPreviewEscapeBound !== "true") {
    document.body.dataset.documentPreviewEscapeBound = "true";
    document.addEventListener("keydown", (event) => {
      if (event.key === "Escape" && documentPreviewModal?.classList.contains("is-open")) {
        closeDocumentPreview();
      }
    });
  }
}

function normalizeDocumentStatusPayload(payload) {
  if (!payload || typeof payload !== "object") {
    return null;
  }

  const documentId = payload.documentId || payload.DocumentId || "";
  if (!documentId) {
    return null;
  }

  return {
    documentId,
    fileName: payload.fileName || payload.FileName || "",
    subject: payload.subject || payload.Subject || "",
    chapter: payload.chapter || payload.Chapter || "",
    status: payload.status || payload.Status || "",
    chunkCount: Number(payload.chunkCount ?? payload.ChunkCount ?? 0),
    indexedAt: payload.indexedAt || payload.IndexedAt || "",
    indexError: payload.indexError || payload.IndexError || ""
  };
}

function normalizeDocumentStatus(status) {
  return (status || "").trim().toLowerCase();
}

function getDocumentStatusLabel(status) {
  switch (normalizeDocumentStatus(status)) {
    case "indexed":
      return "Indexed";
    case "processing":
      return "Processing";
    case "failed":
      return "Failed";
    default:
      return status || "Unknown";
  }
}

function getDocumentStatusClass(status) {
  switch (normalizeDocumentStatus(status)) {
    case "indexed":
      return "is-success";
    case "processing":
      return "is-running";
    case "failed":
      return "is-error";
    default:
      return "is-muted";
  }
}

function getDocumentStatusTitle(document) {
  if (normalizeDocumentStatus(document.status) === "failed" && document.indexError) {
    return document.indexError;
  }

  if (normalizeDocumentStatus(document.status) === "indexed" && document.indexedAt) {
    return `Indexed at ${formatPreviewDate(document.indexedAt)}`;
  }

  return getDocumentStatusLabel(document.status);
}

function updateDocumentKpiValue(key, delta) {
  const target = document.querySelector(`[data-document-kpi="${key}"]`);
  if (!target || delta === 0) {
    return;
  }

  const currentValue = Number.parseInt(target.textContent || "0", 10);
  target.textContent = String(Math.max(0, (Number.isNaN(currentValue) ? 0 : currentValue) + delta));
}

function updateDocumentKpis(previousStatus, nextStatus) {
  const previousKey = normalizeDocumentStatus(previousStatus);
  const nextKey = normalizeDocumentStatus(nextStatus);
  if (!previousKey || previousKey === nextKey) {
    return;
  }

  if (["indexed", "processing", "failed"].includes(previousKey)) {
    updateDocumentKpiValue(previousKey, -1);
  }

  if (["indexed", "processing", "failed"].includes(nextKey)) {
    updateDocumentKpiValue(nextKey, 1);
  }
}

function updateDocumentPreviewAction(row, document) {
  const action = row.querySelector("[data-document-preview-action]");
  if (!action) {
    return;
  }

  const isIndexed = normalizeDocumentStatus(document.status) === "indexed";
  action.disabled = !isIndexed;
  action.textContent = action.dataset.documentPreviewLabel || action.textContent || "View";
  action.title = isIndexed ? "" : getDocumentStatusTitle(document);
  if (isIndexed) {
    action.dataset.documentPreviewUrl = `/Home/DocumentPreview/${document.documentId}`;
  } else {
    delete action.dataset.documentPreviewUrl;
  }
}

function normalizeDocumentProgressPercent(value) {
  return Number.isFinite(value) ? Math.max(0, Math.min(100, value)) : 0;
}

function getDocumentProgressStageMeta(stage) {
  switch ((stage || "").trim().toLowerCase()) {
    case "queued":
      return { label: "Queued", tone: "is-queued" };
    case "extracting":
      return { label: "Extracting", tone: "is-extracting" };
    case "chunking":
      return { label: "Chunking", tone: "is-chunking" };
    case "embedding":
      return { label: "Embedding", tone: "is-embedding" };
    case "saving":
      return { label: "Saving", tone: "is-saving" };
    case "completed":
      return { label: "Completed", tone: "is-completed" };
    default:
      return { label: stage || "Processing", tone: "is-processing" };
  }
}

function formatDocumentIndexProgress(progress) {
  if (!progress) {
    return null;
  }

  const stageMeta = getDocumentProgressStageMeta(progress.stage);
  const percent = normalizeDocumentProgressPercent(progress.progressPercent);
  const message = (progress.message || "").trim();

  return {
    stage: stageMeta.label,
    tone: stageMeta.tone,
    percent,
    message,
    summary: message ? `${stageMeta.label} ${percent}% - ${message}` : `${stageMeta.label} ${percent}%`
  };
}

function applyDocumentProgressToRow(row, progress) {
  if (!row) {
    return;
  }

  const statusBadge = row.querySelector("[data-document-status]");
  const progressRow = window.document.querySelector(`tr[data-document-progress-row="${CSS.escape(row.dataset.documentId)}"]`);
  const progressElement = progressRow ? progressRow.querySelector("[data-document-progress]") : row.querySelector("[data-document-progress]");
  const progressState = formatDocumentIndexProgress(progress);
  if (!progressElement || !progressState) {
    return;
  }

  if (statusBadge) {
    statusBadge.title = progressState.message || progressState.summary;
  }

  if (progressRow) {
    progressRow.hidden = false;
  }
  progressElement.hidden = false;
  progressElement.dataset.progressTone = progressState.tone;
  progressElement.title = progressState.message || progressState.summary;
  progressElement.innerHTML = [
    '<span class="ops-document-progress__meta">',
    '  <span class="ops-document-progress__stage">' + escapeHtml(progressState.stage) + '</span>',
    '  <span class="ops-document-progress__percent">' + progressState.percent + '%</span>',
    '</span>',
    '<span class="ops-document-progress__bar" aria-hidden="true"><span class="ops-document-progress__fill" style="width:' + progressState.percent + '%"></span></span>',
    progressState.message
      ? '<span class="ops-document-progress__message">' + escapeHtml(progressState.message) + '</span>'
      : ''
  ].join("");

  row.dataset.documentProgressStage = progressState.stage;
  row.dataset.documentProgressPercent = String(progressState.percent);
}

function clearDocumentProgress(row) {
  if (!row) {
    return;
  }

  const progressRow = window.document.querySelector(`tr[data-document-progress-row="${CSS.escape(row.dataset.documentId)}"]`);
  const progressElement = progressRow ? progressRow.querySelector("[data-document-progress]") : row.querySelector("[data-document-progress]");
  if (progressElement) {
    progressElement.innerHTML = "";
    progressElement.hidden = true;
    delete progressElement.dataset.progressTone;
    progressElement.removeAttribute("title");
  }

  if (progressRow) {
    progressRow.hidden = true;
  }

  delete row.dataset.documentProgressStage;
  delete row.dataset.documentProgressPercent;
}

function applyDocumentIndexProgressChanged(payload) {
  if (!payload?.documentId) {
    return;
  }

  const rows = [...window.document.querySelectorAll(`[data-document-id="${CSS.escape(payload.documentId)}"]`)];
  rows.forEach((row) => {
    applyDocumentProgressToRow(row, payload);
    row.classList.add("is-realtime-updated");
    window.setTimeout(() => row.classList.remove("is-realtime-updated"), 1400);
  });
}

function applyOnlineUsersWidgetState() {
  if (!onlineUsersWidget || !onlineUsersToggle) {
    return;
  }

  const collapsed = localStorage.getItem(onlineUsersWidgetStateKey) === "true";
  onlineUsersWidget.classList.toggle("is-collapsed", collapsed);
  onlineUsersToggle.setAttribute("aria-expanded", String(!collapsed));
  onlineUsersToggle.setAttribute("aria-label", collapsed ? t("onlineUsers.expand") : t("onlineUsers.collapse"));
  const icon = onlineUsersToggle.querySelector(".material-symbols-outlined");
  if (icon) {
    icon.textContent = collapsed ? "expand_less" : "expand_more";
  }
}

function initOnlineUsersWidget() {
  if (!onlineUsersWidget || !onlineUsersToggle) {
    return;
  }

  applyOnlineUsersWidgetState();
  onlineUsersToggle.addEventListener("click", () => {
    const nextCollapsed = !onlineUsersWidget.classList.contains("is-collapsed");
    localStorage.setItem(onlineUsersWidgetStateKey, String(nextCollapsed));
    applyOnlineUsersWidgetState();
  });
}
function updateDocumentRow(row, document) {
  row.dataset.documentStatusValue = document.status;

  const statusBadge = row.querySelector("[data-document-status]");
  if (statusBadge) {
    statusBadge.classList.remove("is-success", "is-running", "is-error", "is-muted");
    statusBadge.classList.add(getDocumentStatusClass(document.status));
    statusBadge.textContent = getDocumentStatusLabel(document.status);
    statusBadge.title = getDocumentStatusTitle(document);
  }

  const chunks = row.querySelector("[data-document-chunks]");
  if (chunks) {
    chunks.textContent = `${document.chunkCount || 0} chunks`;
  }

  const treeMeta = row.querySelector("[data-document-tree-meta]");
  if (treeMeta) {
    const sizeLabel = row.dataset.documentSizeLabel || "";
    treeMeta.textContent = [getDocumentStatusLabel(document.status), `${document.chunkCount || 0} chunks`, sizeLabel]
      .filter(Boolean)
      .join(" · ");
  }

  clearDocumentProgress(row);
  updateDocumentPreviewAction(row, document);
  row.classList.add("is-realtime-updated");
  window.setTimeout(() => row.classList.remove("is-realtime-updated"), 1400);
}

function applyDocumentStatusChanged(payload) {
  const document = normalizeDocumentStatusPayload(payload);
  if (!document) {
    return;
  }

  const rows = [...window.document.querySelectorAll(`[data-document-id="${CSS.escape(document.documentId)}"]`)];
  if (rows.length === 0) {
    return;
  }

  const previousStatus = rows[0].dataset.documentStatusValue || rows[0].querySelector("[data-document-status]")?.textContent || "";
  updateDocumentKpis(previousStatus, document.status);
  rows.forEach((row) => updateDocumentRow(row, document));
  bindDocumentPreviewButtons();
}

async function startDocumentStatusRealtime() {
  if (!window.signalR?.HubConnectionBuilder || (!documentsPage && !onlineUsersWidget)) {
    return;
  }

  const connection = new window.signalR.HubConnectionBuilder()
    .withUrl("/hubs/documents")
    .withAutomaticReconnect()
    .build();

  if (documentsPage) {
    connection.on("documentStatusChanged", applyDocumentStatusChanged);
    connection.on("documentIndexProgressChanged", applyDocumentIndexProgressChanged);
  }

  if (onlineUsersWidget) {
    connection.on("onlineUsersChanged", applyOnlineUsersChanged);
    connection.onreconnecting(() => setOnlineUsersConnectionState("reconnecting"));
    connection.onreconnected(async () => {
      setOnlineUsersConnectionState("connected");
      try {
        applyOnlineUsersChanged(await connection.invoke("GetOnlineUsersAsync"));
      } catch {
        // Ignore refresh failures after reconnect.
      }
    });
    connection.onclose(() => setOnlineUsersConnectionState("disconnected"));
  }

  async function start() {
    try {
      await connection.start();
      if (onlineUsersWidget) {
        setOnlineUsersConnectionState("connected");
        try {
          applyOnlineUsersChanged(await connection.invoke("GetOnlineUsersAsync"));
        } catch {
          // Initial snapshot is best effort only.
        }
      }
    } catch {
      if (onlineUsersWidget) {
        setOnlineUsersConnectionState("disconnected");
      }
      window.setTimeout(start, 5000);
    }
  }

  await start();
}
async function refreshSessionList() {
  if (!chatSessionList) {
    return;
  }

  try {
    const response = await fetch("/Home/ChatSessions");
    if (!response.ok) {
      return;
    }

    renderSessionList(await response.json());
  } catch {
    // Session history is helpful, but chat should still work if it cannot refresh.
  }
}

async function loadChatSession(sessionId) {
  if (!sessionId) {
    return;
  }

  const response = await fetch(`/Home/ChatSession/${sessionId}`);
  if (!response.ok) {
    return;
  }

  const session = await response.json();
  setSessionId(session.id);
  if (activeSessionTitle) {
    activeSessionTitle.textContent = getSessionTitle(session);
  }
  renderSessionMessages(session.messages || []);
  questionInput?.focus();
}

async function postSessionJson(url, body) {
  const verificationToken = chatForm
    ?.querySelector('input[name="__RequestVerificationToken"]')
    ?.value || "";
  const response = await fetch(url, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "RequestVerificationToken": verificationToken
    },
    body: JSON.stringify(body)
  });
  const payload = await response.json().catch(() => ({}));
  if (!response.ok) {
    throw new Error(payload.error || t("chat.sessionActionError"));
  }

  return payload;
}

async function renameChatSession(sessionId) {
  const currentTitle = document
    .querySelector(`.chat-session-item[data-session-id="${CSS.escape(sessionId)}"] span`)
    ?.textContent
    ?.trim() || "";
  const title = window.prompt(t("chat.renamePrompt"), currentTitle);
  if (title === null) {
    return;
  }

  const normalizedTitle = title.trim();
  if (!normalizedTitle) {
    return;
  }

  const session = await postSessionJson("/Home/RenameChatSession", { sessionId, title: normalizedTitle });
  if (getSessionId() === session.id && activeSessionTitle) {
    activeSessionTitle.textContent = getSessionTitle(session);
  }
  await refreshSessionList();
}

async function toggleChatSessionStar(sessionId, isCurrentlyStarred) {
  await postSessionJson("/Home/StarChatSession", {
    sessionId,
    isStarred: !isCurrentlyStarred
  });
  await refreshSessionList();
}

async function deleteChatSession(sessionId) {
  if (!await showAppConfirm(t("chat.deleteConfirm"))) {
    return;
  }

  await postSessionJson("/Home/DeleteChatSession", { sessionId });
  if (getSessionId() === sessionId) {
    try {
      const session = await postSessionJson("/Home/CreateChatSession", {});
      setSessionId(session.id);
    } catch {
      setSessionId(createSessionId());
    }

    if (activeSessionTitle) {
      activeSessionTitle.textContent = t("chat.defaultSessionTitle");
    }
    renderWelcomeMessage();
  }

  await refreshSessionList();
}

function bindSessionButtons() {
  document.querySelectorAll(".chat-session-item").forEach((button) => {
    button.addEventListener("click", () => {
      loadChatSession(button.dataset.sessionId);
    });
  });

  document.querySelectorAll("[data-session-menu-toggle]").forEach((button) => {
    button.addEventListener("click", (event) => {
      event.stopPropagation();
      const entry = button.closest("[data-session-entry]");
      const menu = entry?.querySelector("[data-session-menu]");
      if (!menu) {
        return;
      }

      const willOpen = !menu.classList.contains("is-open");
      closeSessionMenus(menu);
      menu.classList.toggle("is-open", willOpen);
    });
  });

  document.querySelectorAll("[data-session-action]").forEach((button) => {
    button.addEventListener("click", async (event) => {
      event.stopPropagation();
      const sessionId = button.dataset.sessionId;
      const action = button.dataset.sessionAction;
      closeSessionMenus();
      if (!sessionId || !action) {
        return;
      }

      try {
        if (action === "rename") {
          await renameChatSession(sessionId);
        } else if (action === "star") {
          await toggleChatSessionStar(sessionId, button.dataset.sessionStarred === "true");
        } else if (action === "delete") {
          await deleteChatSession(sessionId);
        }
      } catch (error) {
        appendMessageTo(chatMessages, "assistant", error.message || t("chat.sessionActionError"));
      }
    });
  });
}

const chatTimeGroupWindowMs = 60 * 60 * 1000;

function parseChatTimestamp(value) {
  if (value instanceof Date && !Number.isNaN(value.getTime())) return value;
  const parsed = new Date(value);
  return Number.isNaN(parsed.getTime()) ? null : parsed;
}

function isSameLocalDate(left, right) {
  return left.getFullYear() === right.getFullYear()
    && left.getMonth() === right.getMonth()
    && left.getDate() === right.getDate();
}

function formatChatTimestamp(value) {
  const timestamp = parseChatTimestamp(value);
  if (!timestamp) return "";
  const language = getLanguage();
  const now = new Date();
  const time = timestamp.toLocaleTimeString(language === "vi" ? "vi-VN" : "en-US", {
    hour: "2-digit",
    minute: "2-digit"
  });
  if (isSameLocalDate(timestamp, now)) {
    return language === "vi" ? `Hôm nay, ${time}` : `Today, ${time}`;
  }
  return timestamp.toLocaleString(language === "vi" ? "vi-VN" : "en-US", {
    day: "2-digit",
    month: "2-digit",
    year: "numeric",
    hour: "2-digit",
    minute: "2-digit"
  });
}

function appendChatTimeDivider(target, timestamp, beforeElement = null) {
  const divider = document.createElement("div");
  divider.className = "chat-time-divider";
  divider.dataset.chatGroupTime = timestamp.toISOString();
  const time = document.createElement("time");
  time.dateTime = timestamp.toISOString();
  time.textContent = formatChatTimestamp(timestamp);
  divider.appendChild(time);
  target.insertBefore(divider, beforeElement);
}

function appendChatTimeDividerIfNeeded(target, timestamp) {
  const previousMessage = Array.from(target.querySelectorAll(".message[data-created-at]")).at(-1);
  const previousTimestamp = parseChatTimestamp(previousMessage?.dataset.createdAt);
  if (!previousTimestamp
      || !isSameLocalDate(previousTimestamp, timestamp)
      || timestamp.getTime() - previousTimestamp.getTime() >= chatTimeGroupWindowMs) {
    appendChatTimeDivider(target, timestamp);
  }
}

function refreshChatTimeLabels(root = document) {
  root.querySelectorAll(".chat-time-divider").forEach((divider) => {
    const timestamp = parseChatTimestamp(divider.dataset.chatGroupTime);
    const time = divider.querySelector("time");
    if (timestamp && time) time.textContent = formatChatTimestamp(timestamp);
  });
}

function initializeChatTimeline(target) {
  if (!target) return;
  target.querySelectorAll(".chat-time-divider").forEach((divider) => divider.remove());
  const fallbackTimestamp = new Date();
  let previousTimestamp = null;
  target.querySelectorAll(".message").forEach((message) => {
    const timestamp = parseChatTimestamp(message.dataset.createdAt) || fallbackTimestamp;
    message.dataset.createdAt = timestamp.toISOString();
    if (!previousTimestamp
        || !isSameLocalDate(previousTimestamp, timestamp)
        || timestamp.getTime() - previousTimestamp.getTime() >= chatTimeGroupWindowMs) {
      appendChatTimeDivider(target, timestamp, message);
    }
    previousTimestamp = timestamp;
  });
}

function appendMessageTo(target, role, content, citations = [], options = {}) {
  if (!target) {
    return null;
  }

  if (role === "user") {
    target.querySelector(".chat-empty-state")?.remove();
  }

  const createdAt = options.createdAt === null
    ? null
    : parseChatTimestamp(options.createdAt ?? new Date()) || new Date();
  if (createdAt) appendChatTimeDividerIfNeeded(target, createdAt);

  const wrapper = document.createElement("div");
  wrapper.className = `message ${role}`;
  if (createdAt) wrapper.dataset.createdAt = createdAt.toISOString();

  const bubble = document.createElement("div");
  bubble.className = "bubble";
  if (role === "assistant") {
    renderAssistantContent(bubble, content, {
      includeAnswerLabel: options.includeAnswerLabel !== false,
      includeFollowUps: options.includeFollowUps !== false
    });
  }
  else bubble.textContent = content;

  wrapper.appendChild(bubble);
  appendCitationsToMessage(wrapper, citations);
  target.appendChild(wrapper);
  target.scrollTop = target.scrollHeight;
  return wrapper;
}

function appendSafeInlineContent(target, text) {
  const value = String(text || "");
  const tokenPattern = /(\*\*[^*]+\*\*|`[^`]+`|\[(?:[1-9]|1\d|20)\])/g;
  let cursor = 0;
  for (const match of value.matchAll(tokenPattern)) {
    if (match.index > cursor) target.appendChild(document.createTextNode(value.slice(cursor, match.index)));
    const token = match[0];
    let element;
    if (/^\[\d+\]$/.test(token)) {
      const sourceNumber = Number(token.slice(1, -1));
      element = document.createElement("button");
      element.type = "button";
      element.className = "citation-inline-marker";
      element.textContent = token;
      element.setAttribute("aria-label", getLanguage() === "vi" ? `Mở nguồn ${sourceNumber}` : `Open source ${sourceNumber}`);
      element.addEventListener("click", () => {
        const message = element.closest(".message");
        const sourceDrawer = message?.querySelector("details.citation-cards");
        const source = message?.querySelector(`.citation-card[data-citation-index="${sourceNumber}"]`);
        if (!sourceDrawer || !source) return;
        sourceDrawer.open = true;
        source.open = true;
        source.scrollIntoView({ behavior: "smooth", block: "nearest" });
        source.querySelector("summary")?.focus({ preventScroll: true });
      });
    } else {
      element = document.createElement(token.startsWith("**") ? "strong" : "code");
      element.textContent = token.startsWith("**") ? token.slice(2, -2) : token.slice(1, -1);
    }
    target.appendChild(element);
    cursor = match.index + token.length;
  }
  if (cursor < value.length) target.appendChild(document.createTextNode(value.slice(cursor)));
}

function getFollowUpSuggestions() {
  const subject = getSelectedSubjectFilter();
  if (getLanguage() === "vi") {
    return subject
      ? [`Giải thích kỹ hơn một ý quan trọng của ${subject}.`, `Tóm tắt các nội dung chính khác của ${subject}.`]
      : ["Giải thích kỹ hơn ý chính vừa nêu.", "Trong tài liệu còn nội dung nào liên quan?"];
  }
  return subject
    ? [`Explain one important point from ${subject} in more detail.`, `Summarize other key topics from ${subject}.`]
    : ["Explain the main point in more detail.", "What related information is available in the documents?"];
}

function appendFollowUpSuggestions(bubble) {
  const section = document.createElement("section");
  section.className = "chat-follow-up-suggestions";
  const heading = document.createElement("strong");
  heading.dataset.chatFollowUpLabel = "";
  heading.textContent = t("chat.followUpLabel");
  const actions = document.createElement("div");
  getFollowUpSuggestions().forEach((suggestion, index) => {
    const button = document.createElement("button");
    button.type = "button";
    button.dataset.chatFollowUpIndex = String(index);
    button.textContent = suggestion;
    button.addEventListener("click", () => {
      if (!questionInput) return;
      questionInput.value = suggestion;
      questionInput.focus();
    });
    actions.appendChild(button);
  });
  section.append(heading, actions);
  bubble.appendChild(section);
}

function refreshChatFollowUpSuggestions() {
  const suggestions = getFollowUpSuggestions();
  document.querySelectorAll("[data-chat-follow-up-label]").forEach((heading) => {
    heading.textContent = t("chat.followUpLabel");
  });
  document.querySelectorAll("[data-chat-follow-up-index]").forEach((button) => {
    const index = Number.parseInt(button.dataset.chatFollowUpIndex || "", 10);
    if (Number.isInteger(index) && suggestions[index]) button.textContent = suggestions[index];
  });
}

function renderAssistantContent(bubble, content, options = {}) {
  if (!bubble) return;
  bubble.replaceChildren();
  bubble.classList.add("chat-answer-prose");
  if (options.includeAnswerLabel !== false) {
    const answerLabel = document.createElement("strong");
    answerLabel.className = "chat-answer-label";
    answerLabel.textContent = t("chat.answerLabel");
    bubble.appendChild(answerLabel);
  }
  const lines = String(content || "").replace(/\r\n?/g, "\n").split("\n");
  let list = null;
  let codeBlock = null;
  const closeList = () => { list = null; };

  lines.forEach((rawLine) => {
    const trimmed = rawLine.trim();
    if (trimmed.startsWith("```")) {
      closeList();
      if (codeBlock) codeBlock = null;
      else {
        const pre = document.createElement("pre");
        codeBlock = document.createElement("code");
        pre.appendChild(codeBlock);
        bubble.appendChild(pre);
      }
      return;
    }
    if (codeBlock) {
      codeBlock.textContent += `${rawLine}\n`;
      return;
    }
    if (!trimmed) {
      closeList();
      return;
    }

    const heading = trimmed.match(/^(#{1,4})\s+(.+)$/);
    if (heading) {
      closeList();
      const element = document.createElement(heading[1].length <= 2 ? "h3" : "h4");
      appendSafeInlineContent(element, heading[2]);
      bubble.appendChild(element);
      return;
    }
    const unordered = trimmed.match(/^[-*•]\s+(.+)$/);
    const ordered = trimmed.match(/^\d+[.)]\s+(.+)$/);
    if (unordered || ordered) {
      const tagName = ordered ? "OL" : "UL";
      if (!list || list.tagName !== tagName) {
        list = document.createElement(tagName.toLowerCase());
        bubble.appendChild(list);
      }
      const item = document.createElement("li");
      appendSafeInlineContent(item, (unordered || ordered)[1]);
      list.appendChild(item);
      return;
    }

    closeList();
    const quote = trimmed.match(/^>\s*(.+)$/);
    const element = document.createElement(quote ? "blockquote" : "p");
    appendSafeInlineContent(element, quote ? quote[1] : trimmed);
    bubble.appendChild(element);
  });

  if (!bubble.childNodes.length) bubble.textContent = String(content || "");
  if (options.includeFollowUps !== false && String(content || "").trim()) {
    appendFollowUpSuggestions(bubble);
  }
}

async function appendAssistantAnswer(target, content, citations) {
  const pace = document.getElementById("responsePace")?.value || "comfortable";
  if (pace === "fast" || !content || window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
    return appendMessageTo(target, "assistant", content, citations);
  }
  const wrapper = appendMessageTo(target, "assistant", "", [], {
    includeAnswerLabel: false,
    includeFollowUps: false
  });
  const bubble = wrapper?.querySelector(".bubble");
  if (!wrapper || !bubble) return wrapper;
  const step = content.length > 800 ? 8 : 4;
  for (let index = 0; index < content.length; index += step) {
    bubble.textContent = content.slice(0, index + step);
    if (index % 32 === 0) target.scrollTop = target.scrollHeight;
    await new Promise((resolve) => window.setTimeout(resolve, 12));
  }
  renderAssistantContent(bubble, content);
  appendCitationsToMessage(wrapper, citations);
  target.scrollTop = target.scrollHeight;
  return wrapper;
}

function appendCitationsToMessage(messageWrapper, citations) {
  const sourceItems = Array.isArray(citations)
    ? citations.filter((citation) => citation && typeof citation === "object")
    : [];

  if (!messageWrapper || sourceItems.length === 0) {
    return;
  }

  // Preserve server ordering so inline markers [n] always open the same source.
  // The server already deduplicates citations; a client-side cap could hide a cited source.
  const compactSources = sourceItems;

  if (compactSources.length === 0) {
    return;
  }

  const list = document.createElement("details");
  list.className = "citations citation-cards";
  list.setAttribute("aria-label", getLanguage() === "vi" ? "Nguồn tham khảo" : "References");

  const label = document.createElement("summary");
  label.className = "citation-label";
  label.textContent = getLanguage() === "vi"
    ? `Nguồn (${compactSources.length})`
    : `Sources (${compactSources.length})`;
  list.appendChild(label);

  const sourceList = document.createElement("div");
  sourceList.className = "citation-card-list";

  compactSources.forEach((citation, index) => {
    const item = document.createElement("details");
    item.className = "citation-card";
    item.dataset.citationIndex = String(index + 1);
    const fileName = citation.fileName || citation.FileName || "";
    const chunkIndex = citation.chunkIndex ?? citation.ChunkIndex;
    const excerpt = citation.excerpt || citation.Excerpt || "";
    const metaValues = [
      citation.subject || citation.Subject,
      citation.chapter || citation.Chapter,
      Number.isInteger(Number(chunkIndex)) ? `Đoạn ${chunkIndex}` : ""
    ].filter(Boolean);
    const safeSource = fileName || metaValues.join(" / ") || (getLanguage() === "vi" ? "Nguồn nội bộ" : "Internal source");

    const summary = document.createElement("summary");
    const order = document.createElement("span");
    order.className = "citation-card__order";
    order.textContent = `[${index + 1}]`;
    const identity = document.createElement("span");
    identity.className = "citation-card__identity";
    const title = document.createElement("strong");
    title.textContent = safeSource;
    const meta = document.createElement("small");
    meta.textContent = metaValues.join(" · ");
    identity.append(title, meta);
    summary.append(order, identity);

    const body = document.createElement("div");
    body.className = "citation-card__body";
    const bodyText = document.createElement("p");
    bodyText.textContent = excerpt || (getLanguage() === "vi"
      ? "Nguồn này đã được dùng để đối chiếu câu trả lời. Nội dung chi tiết phụ thuộc quyền truy cập tài liệu của bạn."
      : "This source was used to verify the answer. Detailed content depends on your document access permissions.");
    body.appendChild(bodyText);
    item.append(summary, body);

    sourceList.appendChild(item);
  });

  list.appendChild(sourceList);

  messageWrapper.appendChild(list);
}

function appendAnswerFeedback(messageWrapper) {
  if (!messageWrapper || messageWrapper.querySelector(".chat-answer-feedback")) return;
  const feedback = document.createElement("div");
  feedback.className = "chat-answer-feedback";
  feedback.setAttribute("aria-label", getLanguage() === "vi" ? "Đánh giá câu trả lời" : "Rate this answer");

  const prompt = document.createElement("span");
  prompt.textContent = getLanguage() === "vi" ? "Câu trả lời này có hữu ích không?" : "Was this answer helpful?";
  const status = document.createElement("span");
  status.className = "chat-answer-feedback__status";
  status.setAttribute("role", "status");
  status.setAttribute("aria-live", "polite");
  feedback.append(prompt);

  [["up", "thumb_up", "Hữu ích", "Helpful"], ["down", "thumb_down", "Chưa hữu ích", "Not helpful"]].forEach(([value, icon, vi, en]) => {
    const button = document.createElement("button");
    button.type = "button";
    button.className = "chat-answer-feedback__button";
    button.setAttribute("aria-pressed", "false");
    button.innerHTML = `<span class="material-symbols-outlined" aria-hidden="true">${icon}</span><span>${getLanguage() === "vi" ? vi : en}</span>`;
    button.addEventListener("click", () => {
      feedback.querySelectorAll("button").forEach((item) => item.setAttribute("aria-pressed", String(item === button)));
      status.textContent = getLanguage() === "vi"
        ? "Đã ghi nhận phản hồi trên thiết bị này."
        : "Feedback saved on this device.";
      try { localStorage.setItem(`chat-feedback-${getSessionId()}-${Date.now()}`, value); } catch { /* Storage is optional. */ }
    });
    feedback.appendChild(button);
  });
  feedback.appendChild(status);
  messageWrapper.appendChild(feedback);
}

function appendRetryAction(messageWrapper, question) {
  if (!messageWrapper || !question) return;
  const button = document.createElement("button");
  button.type = "button";
  button.className = "chat-retry-button";
  button.textContent = getLanguage() === "vi" ? "Thử lại câu hỏi" : "Retry question";
  button.addEventListener("click", () => {
    if (!questionInput || !chatForm) return;
    questionInput.value = question;
    chatForm.requestSubmit();
  });
  messageWrapper.appendChild(button);
}

function renderClarificationOptions(messageWrapper, options, originalQuestion) {
  const subjects = Array.isArray(options)
    ? options.filter((subject) => typeof subject === "string" && subject.trim().length > 0).slice(0, 6)
    : [];

  if (!messageWrapper || subjects.length === 0) {
    return;
  }

  const actions = document.createElement("div");
  actions.className = "chat-clarification-options";
  subjects.forEach((subject) => {
    const button = document.createElement("button");
    button.type = "button";
    button.className = "chat-clarification-chip";
    button.textContent = subject;
    button.addEventListener("click", () => {
      setSelectedSubjectFilter(subject);
      if (questionInput && chatForm) {
        questionInput.value = originalQuestion;
        chatForm.requestSubmit();
      }
    });
    actions.appendChild(button);
  });

  messageWrapper.appendChild(actions);
  messageWrapper.parentElement.scrollTop = messageWrapper.parentElement.scrollHeight;
}

async function submitChatQuestion(input, messagesTarget, focusAfter = true) {
  const question = input?.value.trim();
  if (!question || !messagesTarget) {
    return false;
  }

  rememberAskedQuestion(question);
  advanceRelatedRotation();
  updateSuggestionButtons();
  renderRelatedQuestions();
  appendMessageTo(messagesTarget, "user", question);
  input.value = "";
  const loadingMessage = appendMessageTo(messagesTarget, "assistant", t("chat.loading"), [], {
    createdAt: null,
    includeAnswerLabel: false,
    includeFollowUps: false
  });

  try {
    const response = await fetch("/Home/Ask", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "RequestVerificationToken": chatForm?.querySelector('input[name="__RequestVerificationToken"]')?.value || ""
      },
      body: JSON.stringify({
        sessionId: getSessionId(),
        question,
        subjectFilter: getSelectedSubjectFilter(),
        language: getLanguage(),
        responsePace: document.getElementById("responsePace")?.value || "comfortable",
        answerDepth: document.getElementById("answerDepth")?.value || "balanced"
      })
    });

    const payload = await response.json();
    loadingMessage?.remove();

    if (!response.ok) {
      const errorMessage = appendMessageTo(messagesTarget, "assistant", payload.error || t("chat.requestError"));
      appendRetryAction(errorMessage, question);
      return false;
    }

    setSessionId(payload.sessionId);
    const remainingQuestions = document.getElementById("questionsRemaining");
    if (remainingQuestions && Number.isInteger(payload.questionsRemaining)) {
      remainingQuestions.textContent = new Intl.NumberFormat(getLanguage() === "vi" ? "vi-VN" : "en-US").format(payload.questionsRemaining);
    }
    rememberAskedQuestion(question);
    if (activeSessionTitle && (!activeSessionTitle.textContent?.trim() || activeSessionTitle.textContent.trim() === t("chat.defaultSessionTitle"))) {
      activeSessionTitle.textContent = question.length <= 56 ? question : `${question.slice(0, 56)}...`;
    }
    const answerMessage = await appendAssistantAnswer(messagesTarget, payload.answer, payload.citations || []);
    appendAnswerFeedback(answerMessage);
    if (payload.needsClarification && Array.isArray(payload.subjectOptions)) {
      renderClarificationOptions(answerMessage, payload.subjectOptions, question);
    }

    advanceRelatedRotation();
    renderRelatedQuestions();
    refreshSessionList();
    return true;
  } catch {
    loadingMessage?.remove();
    const errorMessage = appendMessageTo(messagesTarget, "assistant", t("chat.connectionError"));
    appendRetryAction(errorMessage, question);
    return false;
  } finally {
    if (focusAfter) {
      input.focus();
    }
  }
}

document.querySelectorAll("[data-language-option]").forEach((button) => {
  button.addEventListener("click", () => {
    setLanguage(button.dataset.languageOption);
  });
});

document.addEventListener("click", (event) => {
  if (!event.target.closest?.("[data-session-entry]")) {
    closeSessionMenus();
  }
});

document.addEventListener("keydown", (event) => {
  if (event.key === "Escape") {
    closeSessionMenus();
  }
});

if (newSessionButton) {
  newSessionButton.addEventListener("click", async () => {
    try {
      const session = await postSessionJson("/Home/CreateChatSession", {});
      setSessionId(session.id);
    } catch {
      setSessionId(createSessionId());
    }

    if (activeSessionTitle) {
      activeSessionTitle.textContent = t("chat.defaultSessionTitle");
    }
    renderWelcomeMessage();
    refreshSessionList();
  });
}

if (documentDropzone && documentFileInput && documentFileName) {
  function updateSelectedFileName(files) {
    const file = files?.[0];
    documentFileName.textContent = file ? file.name : t("documents.dropzoneDefault");
    documentDropzone.classList.toggle("has-file", Boolean(file));
  }

  documentFileInput.addEventListener("change", () => {
    updateSelectedFileName(documentFileInput.files);
  });

  ["dragenter", "dragover"].forEach((eventName) => {
    documentDropzone.addEventListener(eventName, (event) => {
      event.preventDefault();
      documentDropzone.classList.add("is-dragover");
    });
  });

  ["dragleave", "drop"].forEach((eventName) => {
    documentDropzone.addEventListener(eventName, () => {
      documentDropzone.classList.remove("is-dragover");
    });
  });

  documentDropzone.addEventListener("drop", (event) => {
    event.preventDefault();
    if (event.dataTransfer?.files?.length) {
      documentFileInput.files = event.dataTransfer.files;
      updateSelectedFileName(documentFileInput.files);
    }
  });
}

function initAssistantLauncherDrag() {
  if (!assistantLauncher || !assistantLauncherButton) {
    return;
  }

  let startX = 0;
  let startY = 0;
  let startLeft = 0;
  let startTop = 0;
  let lastX = 0;
  let didDrag = false;
  let suppressClick = false;

  function clamp(value, min, max) {
    return Math.min(Math.max(value, min), max);
  }

  function moveLauncher(left, top, deltaX) {
    const rect = assistantLauncher.getBoundingClientRect();
    const maxLeft = window.innerWidth - rect.width - 10;
    const maxTop = window.innerHeight - rect.height - 10;
    assistantLauncher.style.left = `${clamp(left, 10, maxLeft)}px`;
    assistantLauncher.style.top = `${clamp(top, 10, maxTop)}px`;
    assistantLauncher.style.right = "auto";
    assistantLauncher.style.bottom = "auto";
    assistantLauncher.style.setProperty("--launcher-stretch-x", String(1 + Math.min(Math.abs(deltaX) / 160, 0.16)));
    assistantLauncher.style.setProperty("--launcher-stretch-y", String(1 - Math.min(Math.abs(deltaX) / 240, 0.08)));
    assistantLauncher.style.setProperty("--launcher-rotate", `${clamp(deltaX / 12, -8, 8)}deg`);
  }

  assistantLauncherButton.addEventListener("pointerdown", (event) => {
    if (event.button !== 0) {
      return;
    }

    const rect = assistantLauncher.getBoundingClientRect();
    startX = event.clientX;
    startY = event.clientY;
    lastX = event.clientX;
    startLeft = rect.left;
    startTop = rect.top;
    didDrag = false;
    event.preventDefault();
    assistantLauncher.classList.add("is-dragging");
    assistantLauncher.classList.remove("is-released");
    assistantLauncherButton.setPointerCapture?.(event.pointerId);
  });

  assistantLauncherButton.addEventListener("pointermove", (event) => {
    if (!assistantLauncher.classList.contains("is-dragging")) {
      return;
    }

    const deltaX = event.clientX - startX;
    const deltaY = event.clientY - startY;
    if (Math.hypot(deltaX, deltaY) > 5) {
      didDrag = true;
    }

    if (!didDrag) {
      return;
    }

    event.preventDefault();
    moveLauncher(startLeft + deltaX, startTop + deltaY, event.clientX - lastX);
    lastX = event.clientX;
  });

  function finishDrag(event) {
    if (!assistantLauncher.classList.contains("is-dragging")) {
      return;
    }

    assistantLauncher.classList.remove("is-dragging");
    assistantLauncher.classList.add("is-released");
    assistantLauncher.style.setProperty("--launcher-stretch-x", "1");
    assistantLauncher.style.setProperty("--launcher-stretch-y", "1");
    assistantLauncher.style.setProperty("--launcher-rotate", "0deg");
    assistantLauncherButton.releasePointerCapture?.(event.pointerId);

    if (didDrag) {
      suppressClick = true;
      window.setTimeout(() => {
        suppressClick = false;
      }, 0);
    }

    window.setTimeout(() => {
      assistantLauncher.classList.remove("is-released");
    }, 280);
  }

  assistantLauncherButton.addEventListener("pointerup", finishDrag);
  assistantLauncherButton.addEventListener("pointercancel", finishDrag);
  assistantLauncherButton.addEventListener("click", (event) => {
    event.preventDefault();

    const clickDistance = Math.hypot(event.clientX - startX, event.clientY - startY);
    if (suppressClick || clickDistance > 5) {
      return;
    }

    window.location.href = assistantLauncherButton.href;
  });
}

function bindSuggestionButtons() {
  document.querySelectorAll("[data-question]").forEach((button) => {
    if (button.dataset.questionBound === "true") {
      return;
    }

    button.dataset.questionBound = "true";
    button.addEventListener("click", () => {
      if (!questionInput) {
        return;
      }

      questionInput.value = button.dataset.question || "";
      questionInput.focus();
      rememberAskedQuestion(questionInput.value);
      updateSuggestionButtons();
      renderRelatedQuestions();
      if (button.classList.contains("related-question-chip")) {
        chatForm?.requestSubmit();
      }
    });
  });
}

function initUploadSourceSwitchers() {
  document.querySelectorAll(".ops-upload-form").forEach((form) => {
    const choices = Array.from(form.querySelectorAll("[data-upload-source-choice]"));
    const panels = Array.from(form.querySelectorAll("[data-upload-source-panel]"));
    const fileInput = form.querySelector('input[name="File"]');
    const urlInput = form.querySelector('input[name="SourceUrl"]');
    if (!choices.length || !panels.length || !fileInput || !urlInput) return;

    const setSource = (source) => {
      choices.forEach((choice) => {
        const isActive = choice.dataset.uploadSourceChoice === source;
        choice.classList.toggle("is-active", isActive);
        choice.setAttribute("aria-pressed", String(isActive));
      });
      panels.forEach((panel) => {
        panel.hidden = panel.dataset.uploadSourcePanel !== source;
      });
      fileInput.required = source === "file";
      urlInput.required = source === "url";
      if (source === "file") {
        urlInput.value = "";
      } else {
        fileInput.value = "";
        updateDropzoneDefaultText();
      }
    };

    choices.forEach((choice) => {
      choice.addEventListener("click", () => {
        setSource(choice.dataset.uploadSourceChoice || "file");
        const panel = panels.find((item) => !item.hidden);
        panel?.querySelector("input")?.focus();
      });
    });
    form.addEventListener("submit", (event) => {
      const submitButton = event.submitter;
      form.classList.add("is-submitting");
      form.setAttribute("aria-busy", "true");
      form.querySelectorAll("button[type='submit']").forEach((button) => {
        button.disabled = true;
      });
      if (submitButton) {
        submitButton.dataset.loadingLabel = t("documents.indexing");
        submitButton.classList.add("is-loading");
      }
    });
    setSource("file");
  });
}

function initPasswordVisibilityToggles() {
  document.querySelectorAll("[data-password-toggle]").forEach((button) => {
    const input = document.getElementById(button.dataset.passwordToggle || "");
    if (!input) return;
    button.addEventListener("click", () => {
      const isVisible = input.type === "text";
      input.type = isVisible ? "password" : "text";
      button.setAttribute("aria-label", t(isVisible ? "account.showPassword" : "account.hidePassword"));
      const icon = button.querySelector(".material-symbols-outlined");
      if (icon) icon.textContent = isVisible ? "visibility" : "visibility_off";
      input.focus();
      input.setSelectionRange?.(input.value.length, input.value.length);
    });
  });
}

function initCheckoutLoadingStates() {
  document.querySelectorAll("[data-checkout-form]").forEach((form) => {
    form.addEventListener("submit", (event) => {
      const button = event.submitter || form.querySelector("button[type='submit']");
      form.classList.add("is-submitting");
      form.setAttribute("aria-busy", "true");
      if (!button) return;
      button.disabled = true;
      button.setAttribute("aria-busy", "true");
      button.dataset.loadingLabel = t("packages.processing");
      button.classList.add("is-loading");
    });
  });
}

function initPageSkeleton() {
  const skeleton = document.querySelector("[data-page-skeleton]");
  const status = document.querySelector("[data-page-loading-status]");
  const main = document.getElementById("mainContent");
  if (!skeleton) return;

  let visible = false;
  const show = () => {
    if (visible) return;
    visible = true;
    skeleton.hidden = false;
    document.body.classList.add("is-page-loading");
    main?.setAttribute("aria-busy", "true");
    if (status) status.textContent = t("loading.page");
  };

  const hide = () => {
    visible = false;
    skeleton.hidden = true;
    document.body.classList.remove("is-page-loading");
    main?.removeAttribute("aria-busy");
    if (status) status.textContent = "";
  };

  document.addEventListener("click", (event) => {
    if (event.defaultPrevented || event.button !== 0 || event.metaKey || event.ctrlKey || event.shiftKey || event.altKey) return;
    const clickedElement = event.target instanceof Element ? event.target : null;
    const link = clickedElement?.closest("a[href]");
    if (!link || link.hasAttribute("download") || link.dataset.noPageSkeleton !== undefined) return;
    if (link.target && link.target.toLowerCase() !== "_self") return;

    const rawHref = link.getAttribute("href") || "";
    if (!rawHref || rawHref.startsWith("#") || /^(mailto:|tel:|javascript:)/i.test(rawHref)) return;

    const targetUrl = new URL(link.href, window.location.href);
    if (targetUrl.origin !== window.location.origin) return;
    const currentUrl = new URL(window.location.href);
    const changesOnlyHash = targetUrl.pathname === currentUrl.pathname
      && targetUrl.search === currentUrl.search
      && targetUrl.hash !== currentUrl.hash;
    if (!changesOnlyHash) show();
  });

  document.addEventListener("submit", (event) => {
    if (event.defaultPrevented || !(event.target instanceof HTMLFormElement)) return;
    const form = event.target;
    if (form.dataset.noPageSkeleton !== undefined || form.target || form.method.toLowerCase() === "dialog") return;
    show();
  });

  window.addEventListener("beforeunload", show);
  window.addEventListener("pageshow", hide);
}

function initAdminCreateUserForm() {
  document.querySelectorAll("[data-admin-user-role-subject-form]").forEach((form) => {
    if (form.dataset.roleSubjectBound === "true") {
      return;
    }

    const roleSelect = form.querySelector("[data-admin-role-select]");
    const subjectPicker = form.querySelector("[data-lecturer-subject-picker]");
    if (!roleSelect || !subjectPicker) {
      return;
    }

    form.dataset.roleSubjectBound = "true";
    const subjectInputs = Array.from(subjectPicker.querySelectorAll("input[name='SubjectIds']"));
    const syncSubjectPicker = () => {
      const isLecturer = (roleSelect.value || "").toLowerCase() === "lecturer";
      subjectPicker.classList.toggle("is-hidden", !isLecturer);
      subjectInputs.forEach((input) => {
        input.disabled = !isLecturer;
        if (!isLecturer) {
          input.checked = false;
        }
      });
    };

    roleSelect.addEventListener("change", syncSubjectPicker);
    syncSubjectPicker();
  });
}

const modalFocusableSelector = [
  "a[href]",
  "button:not([disabled])",
  "input:not([disabled])",
  "select:not([disabled])",
  "textarea:not([disabled])",
  "[tabindex]:not([tabindex='-1'])"
].join(",");
const modalAccessibilityState = new WeakMap();

function openAccessibleModal(modal, initialFocus, trigger = document.activeElement) {
  if (!modal) return;
  const inerted = [];
  let branch = modal;
  let parent = modal.parentElement;

  modal.hidden = false;
  while (parent) {
    Array.from(parent.children).forEach((sibling) => {
      if (sibling === branch || sibling.tagName === "SCRIPT" || sibling.tagName === "LINK") return;
      inerted.push({ element: sibling, wasInert: sibling.inert });
      sibling.inert = true;
    });
    branch = parent;
    parent = parent.parentElement;
  }

  modalAccessibilityState.set(modal, {
    inerted,
    trigger: trigger instanceof HTMLElement ? trigger : null
  });
  window.requestAnimationFrame(() => {
    const target = initialFocus || modal.querySelector(modalFocusableSelector);
    target?.focus();
  });
}

function closeAccessibleModal(modal, restoreFocus = true) {
  if (!modal) return;
  const state = modalAccessibilityState.get(modal);
  modal.hidden = true;
  state?.inerted.forEach(({ element, wasInert }) => {
    element.inert = wasInert;
  });
  modalAccessibilityState.delete(modal);
  if (restoreFocus) state?.trigger?.focus();
}

function keepFocusInsideModal(event, modal) {
  if (event.key !== "Tab" || !modal || modal.hidden) return;
  const focusable = Array.from(modal.querySelectorAll(modalFocusableSelector))
    .filter((element) => !element.hidden && element.getAttribute("aria-hidden") !== "true");
  if (!focusable.length) {
    event.preventDefault();
    return;
  }

  const first = focusable[0];
  const last = focusable[focusable.length - 1];
  if (event.shiftKey && document.activeElement === first) {
    event.preventDefault();
    last.focus();
  } else if (!event.shiftKey && document.activeElement === last) {
    event.preventDefault();
    first.focus();
  }
}

function showAppConfirm(message) {
  const modal = document.querySelector("[data-app-confirm]");
  if (!modal) {
    console.error("Confirmation dialog is unavailable; the action was cancelled.");
    return Promise.resolve(false);
  }
  const messageElement = modal.querySelector("[data-app-confirm-message]");
  const acceptButton = modal.querySelector("[data-app-confirm-accept]");
  const cancelButtons = Array.from(modal.querySelectorAll("[data-app-confirm-cancel]"));
  const trigger = document.activeElement;
  if (messageElement) messageElement.textContent = message;

  return new Promise((resolve) => {
    const controller = new AbortController();
    const finish = (result) => {
      controller.abort();
      closeAccessibleModal(modal);
      resolve(result);
    };
    const keydown = (event) => {
      if (event.key === "Escape") {
        event.preventDefault();
        finish(false);
        return;
      }
      keepFocusInsideModal(event, modal);
    };

    acceptButton?.addEventListener("click", () => finish(true), { signal: controller.signal });
    cancelButtons.forEach((button) => button.addEventListener("click", () => finish(false), { signal: controller.signal }));
    document.addEventListener("keydown", keydown, { signal: controller.signal });
    openAccessibleModal(modal, acceptButton, trigger);
  });
}

function initConfirmForms() {
  document.querySelectorAll("form[data-confirm], form[data-confirm-en], form[data-confirm-vi]").forEach((form) => {
    if (form.dataset.confirmBound === "true") {
      return;
    }

    form.dataset.confirmBound = "true";
    form.addEventListener("submit", async (event) => {
      if (form.dataset.confirmConfirmed === "true") return;
      event.preventDefault();
      const message = getLanguage() === "vi"
        ? (form.dataset.confirmVi || form.dataset.confirm || "Bạn có chắc muốn tiếp tục?")
        : (form.dataset.confirmEn || form.dataset.confirm || "Are you sure you want to continue?");
      if (!await showAppConfirm(message)) return;
      form.dataset.confirmConfirmed = "true";
      form.requestSubmit();
    });
  });
}

function initSubjectCards() {
  document.querySelectorAll("[data-subject-detail-url]").forEach((card) => {
    if (card.dataset.subjectCardBound === "true") {
      return;
    }

    card.dataset.subjectCardBound = "true";
    card.addEventListener("click", (event) => {
      const url = card.dataset.subjectDetailUrl;
      if (!url) {
        return;
      }

      event.preventDefault();
      window.location.href = url;
    });

    card.addEventListener("keydown", (event) => {
      if (event.key !== "Enter" && event.key !== " ") {
        return;
      }

      const url = card.dataset.subjectDetailUrl;
      if (!url) {
        return;
      }

      event.preventDefault();
      window.location.href = url;
    });
  });
}

function initAdminRoleUpdateForms() {
  document.querySelectorAll("[data-admin-role-update-select]").forEach((select) => {
    if (select.dataset.roleUpdateBound === "true") {
      return;
    }

    select.dataset.roleUpdateBound = "true";
    select.dataset.previousValue = select.value || "";
    select.addEventListener("change", async () => {
      const form = select.closest("[data-admin-role-update-form]");
      if (!form) {
        return;
      }

      const nextRole = select.value || "";
      const userLabel = select.dataset.adminRoleUser || "this user";
      const confirmMessage = t("admin.confirmRoleChange")
        .replace("{user}", userLabel)
        .replace("{role}", nextRole);
      if (!await showAppConfirm(confirmMessage)) {
        select.value = select.dataset.previousValue || "";
        return;
      }

      select.dataset.previousValue = nextRole;
      form.dataset.confirmConfirmed = "true";
      form.requestSubmit();
    });
  });
}

function initPackagePricePublishing() {
  const modal = document.querySelector("[data-package-price-review]");
  const forms = Array.from(document.querySelectorAll("[data-package-price-form]"));
  if (!modal || !forms.length) return;

  const packageName = modal.querySelector("[data-review-package-name]");
  const currentPrice = modal.querySelector("[data-review-current-price]");
  const newPrice = modal.querySelector("[data-review-new-price]");
  const priceDelta = modal.querySelector("[data-review-price-delta]");
  const reason = modal.querySelector("[data-review-reason]");
  const pendingWarning = modal.querySelector("[data-review-pending-warning]");
  const freeWarning = modal.querySelector("[data-review-free-warning]");
  const confirmButton = modal.querySelector("[data-package-price-review-confirm]");
  const cancelButtons = Array.from(modal.querySelectorAll("[data-package-price-review-cancel]"));
  const formatPrice = (value) => `${new Intl.NumberFormat("vi-VN").format(value)}đ`;
  let activeForm = null;
  let triggerButton = null;

  const close = () => {
    document.body.classList.remove("package-price-review-open");
    closeAccessibleModal(modal);
    activeForm = null;
    triggerButton = null;
  };

  const open = (form) => {
    const current = Number(form.dataset.currentPrice || 0);
    const priceInput = form.querySelector('input[name="PriceVnd"]');
    const reasonInput = form.querySelector('textarea[name="Reason"]');
    const next = Number(priceInput?.value || 0);
    const delta = next - current;
    const deltaPercent = current > 0 ? Math.abs(delta) * 100 / current : null;
    const pendingCount = Number(form.dataset.pendingCount || 0);

    activeForm = form;
    triggerButton = form.querySelector('button[type="submit"]');
    packageName.textContent = form.dataset.packageName || "gói dịch vụ";
    currentPrice.textContent = formatPrice(current);
    newPrice.textContent = formatPrice(next);
    priceDelta.textContent = delta === 0
      ? "Không thay đổi"
      : `${delta > 0 ? "Tăng" : "Giảm"} ${formatPrice(Math.abs(delta))}${deltaPercent === null ? "" : ` (${deltaPercent.toFixed(1)}%)`}`;
    reason.textContent = reasonInput?.value.trim() || "";

    pendingWarning.hidden = pendingCount <= 0;
    if (pendingCount > 0) {
      pendingWarning.textContent = `${pendingCount} payment đang chờ vẫn tiếp tục sử dụng số tiền đã được ghi nhận khi tạo checkout.`;
    }
    freeWarning.hidden = !(current > 0 && next === 0);
    document.body.classList.add("package-price-review-open");
    openAccessibleModal(modal, confirmButton, triggerButton);
  };

  forms.forEach((form) => {
    const priceInput = form.querySelector('input[name="PriceVnd"]');
    priceInput?.addEventListener("input", () => priceInput.setCustomValidity(""));

    form.addEventListener("submit", (event) => {
      if (form.dataset.pricePublishConfirmed === "true") return;
      event.preventDefault();
      priceInput?.setCustomValidity("");
      if (!form.reportValidity()) return;
      const current = Number(form.dataset.currentPrice || 0);
      const next = Number(priceInput?.value || 0);
      if (next === current) {
        priceInput?.setCustomValidity("Giá mới phải khác giá hiện tại.");
        priceInput?.reportValidity();
        return;
      }
      open(form);
    });
  });

  cancelButtons.forEach((button) => button.addEventListener("click", close));
  confirmButton?.addEventListener("click", () => {
    if (!activeForm) return;
    const current = Number(activeForm.dataset.currentPrice || 0);
    const next = Number(activeForm.querySelector('input[name="PriceVnd"]')?.value || 0);
    const confirmFreePrice = activeForm.querySelector("[data-confirm-free-price]");
    if (confirmFreePrice) confirmFreePrice.value = current > 0 && next === 0 ? "true" : "false";
    activeForm.dataset.pricePublishConfirmed = "true";
    document.body.classList.remove("package-price-review-open");
    closeAccessibleModal(modal, false);
    activeForm.requestSubmit();
  });
  document.addEventListener("keydown", (event) => {
    if (modal.hidden) return;
    if (event.key === "Escape") {
      event.preventDefault();
      close();
      return;
    }
    keepFocusInsideModal(event, modal);
  });
}

function initSystemNotifications() {
  const modals = Array.from(document.querySelectorAll("[data-system-notification]"));
  if (!modals.length) return;

  const pendingModals = modals.filter((modal) => {
    const notificationId = modal.dataset.notificationId;
    const viewerId = modal.dataset.viewerId || "anonymous";
    const revision = modal.dataset.notificationRevision;
    if (!notificationId) return false;

    const storageKey = `courseAssistant.systemNotification.${viewerId}.${notificationId}${revision ? `.${revision}` : ""}`;
    modal.dataset.storageKey = storageKey;
    try {
      return localStorage.getItem(storageKey) !== "acknowledged";
    } catch {
      // The notification still works when browser storage is unavailable.
      return true;
    }
  });

  let activeModal = null;
  let notificationTrigger = null;
  const showNext = () => {
    activeModal = pendingModals.shift() || null;
    if (!activeModal) {
      document.body.classList.remove("price-change-modal-open");
      return;
    }

    notificationTrigger ??= document.activeElement;
    document.body.classList.add("price-change-modal-open");
    openAccessibleModal(
      activeModal,
      activeModal.querySelector(".price-change-modal__dialog button"),
      notificationTrigger);
  };

  const close = (modal) => {
    const hasNext = pendingModals.length > 0;
    closeAccessibleModal(modal, !hasNext);
    try {
      localStorage.setItem(modal.dataset.storageKey, "acknowledged");
    } catch {
      // Dismiss for this page view when browser storage is unavailable.
    }

    if (activeModal === modal) {
      if (!hasNext) notificationTrigger = null;
      showNext();
    }
  };

  modals.forEach((modal) => {
    const closeButtons = Array.from(modal.querySelectorAll("[data-system-notification-close]"));
    closeButtons.forEach((button) => button.addEventListener("click", () => close(modal)));
  });

  document.addEventListener("keydown", (event) => {
    if (!activeModal || activeModal.hidden) return;
    if (event.key === "Escape") {
      event.preventDefault();
      close(activeModal);
      return;
    }
    keepFocusInsideModal(event, activeModal);
  });
  showNext();
}

bindSuggestionButtons();
bindSessionButtons();
bindSubjectFilterChips();
applyInitialSubjectFromUrl();
bindDocumentPreviewButtons();
initSubjectCards();
initUploadSourceSwitchers();
initPasswordVisibilityToggles();
initCheckoutLoadingStates();
initPageSkeleton();
initAdminCreateUserForm();
initConfirmForms();
initAdminRoleUpdateForms();
initPackagePricePublishing();
initSystemNotifications();
initAssistantLauncherDrag();
initOnlineUsersWidget();
markActiveSession(getSessionId());
initializeChatTimeline(chatMessages);
applyLanguage();
startDocumentStatusRealtime();

if (chatForm) {
  chatForm.addEventListener("submit", async (event) => {
    event.preventDefault();
    if (isSending) {
      return;
    }

    isSending = true;
    try {
      await submitChatQuestion(questionInput, chatMessages);
    } finally {
      isSending = false;
    }
  });
}

if (questionInput && chatForm) {
  questionInput.addEventListener("keydown", (event) => {
    if (event.key === "Enter" && !event.shiftKey) {
      event.preventDefault();
      chatForm.requestSubmit();
    }
  });
}
document.addEventListener("click", (event) => {
  const printButton = event.target.closest("[data-print-receipt]");
  if (!printButton) return;
  const receipt = document.getElementById(printButton.dataset.printReceipt || "");
  if (!receipt) return;
  document.body.classList.add("receipt-printing");
  receipt.classList.add("is-print-target");
  const cleanup = () => {
    document.body.classList.remove("receipt-printing");
    receipt.classList.remove("is-print-target");
    window.removeEventListener("afterprint", cleanup);
  };
  window.addEventListener("afterprint", cleanup);
  window.print();
  window.setTimeout(cleanup, 1000);
});
document.addEventListener("click", (event) => {
  document.querySelectorAll("[data-account-menu][open]").forEach((menu) => {
    if (!menu.contains(event.target)) menu.removeAttribute("open");
  });
});
document.addEventListener("keydown", (event) => {
  if (event.key !== "Escape") return;
  document.querySelectorAll("[data-account-menu][open]").forEach((menu) => {
    menu.removeAttribute("open");
    menu.querySelector("summary")?.focus();
  });
});
if (window.location.hash === "#transactionHistory") {
  const transactionHistory = document.getElementById("transactionHistory");
  if (transactionHistory instanceof HTMLDetailsElement) transactionHistory.open = true;
}
const mobileNavToggle = document.querySelector("[data-mobile-nav-toggle]");
const primaryNavigation = document.querySelector("[data-primary-navigation]");

function setMobileNavigation(open) {
  if (!mobileNavToggle || !primaryNavigation) return;

  document.body.classList.toggle("mobile-nav-open", open);
  mobileNavToggle.setAttribute("aria-expanded", String(open));
  const hiddenLabel = mobileNavToggle.querySelector(".visually-hidden");
  if (hiddenLabel) hiddenLabel.textContent = open ? "Đóng menu chính" : "Mở menu chính";
}

mobileNavToggle?.addEventListener("click", () => {
  setMobileNavigation(mobileNavToggle.getAttribute("aria-expanded") !== "true");
});

primaryNavigation?.addEventListener("click", (event) => {
  const clickedElement = event.target instanceof Element ? event.target : null;
  if (clickedElement?.closest("a") && window.matchMedia("(max-width: 900px)").matches) {
    setMobileNavigation(false);
  }
});

window.addEventListener("resize", () => {
  if (window.innerWidth > 900) setMobileNavigation(false);
});
