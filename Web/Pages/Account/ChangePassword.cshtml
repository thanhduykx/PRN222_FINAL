@page
@model PRN222_FINAL.Web.Pages.Account.ChangePasswordModel
@{
    ViewData["Title"] = "Đổi mật khẩu";
    ViewData["TitleI18n"] = "account.changePasswordTitle";
    ViewData["UseRblShell"] = true;
    ViewData["ShellTitle"] = "Đổi mật khẩu";
    ViewData["ShellTitleI18n"] = "account.changePasswordTitle";
}

<section class="auth-page">
    <partial name="_AuthStars" />
    <div class="auth-panel">
        <p class="eyebrow" data-i18n="account.security">Account security</p>
        <h1 data-i18n="account.changePasswordTitle">Đổi mật khẩu</h1>
        <p class="subtitle" data-i18n="account.changePasswordDesc">Cập nhật mật khẩu đăng nhập nội bộ của bạn.</p>

        @if (!string.IsNullOrWhiteSpace(Model.StatusMessage))
        {
            <div class="alert alert-success">@Model.StatusMessage</div>
        }

        <form asp-page="/Account/ChangePassword" method="post" class="auth-form">
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <label>
                <span data-i18n="account.currentPassword">Mật khẩu hiện tại</span>
                <input asp-for="Input.CurrentPassword" class="form-control" autocomplete="current-password" />
                <span asp-validation-for="Input.CurrentPassword" class="text-danger"></span>
            </label>
            <label>
                <span data-i18n="account.newPassword">Mật khẩu mới</span>
                <input asp-for="Input.NewPassword" class="form-control" autocomplete="new-password" />
                <span asp-validation-for="Input.NewPassword" class="text-danger"></span>
            </label>
            <label>
                <span data-i18n="account.confirmNewPassword">Nhập lại mật khẩu mới</span>
                <input asp-for="Input.ConfirmPassword" class="form-control" autocomplete="new-password" />
                <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
            </label>
            <button type="submit" class="btn btn-primary"><span data-i18n="account.changePasswordTitle">Đổi mật khẩu</span></button>
        </form>
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

