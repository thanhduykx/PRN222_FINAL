@page
@model PRN222_FINAL.Web.Pages.Account.ForgotPasswordModel
@{
    ViewData["Title"] = "Quên mật khẩu";
    ViewData["BodyClass"] = "auth-body";
}

<section class="auth-page">
    <partial name="_AuthStars" />
    <div class="auth-panel">
        <p class="eyebrow">Account recovery</p>
        <h1>Quên mật khẩu</h1>
        <p class="subtitle">Nhập email tài khoản để nhận liên kết đặt lại mật khẩu.</p>

        @if (TempData["AuthError"] is string error)
        {
            <div class="alert alert-danger">@error</div>
        }

        <form asp-page="/Account/ForgotPassword" method="post" class="auth-form">
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <label>
                Email
                <input asp-for="Input.Email" class="form-control" autocomplete="email" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </label>
            <button type="submit" class="btn btn-primary">Gửi liên kết</button>
        </form>

        <p class="auth-switch">Đã nhớ mật khẩu? <a asp-page="/Account/Login">Đăng nhập</a></p>
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

