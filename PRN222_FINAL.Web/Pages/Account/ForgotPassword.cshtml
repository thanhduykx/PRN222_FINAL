@page
@model PRN222_FINAL.Web.Pages.Account.ForgotPasswordModel
@{
    ViewData["Title"] = "QuÃªn máº­t kháº©u";
    ViewData["BodyClass"] = "auth-body";
}

<section class="auth-page">
    <partial name="_AuthStars" />
    <div class="auth-panel">
        <p class="eyebrow">Account recovery</p>
        <h1>QuÃªn máº­t kháº©u</h1>
        <p class="subtitle">Nháº­p email tÃ i khoáº£n Ä‘á»ƒ nháº­n liÃªn káº¿t Ä‘áº·t láº¡i máº­t kháº©u.</p>

        @if (TempData["AuthError"] is string error)
        {
            <div class="alert alert-danger">@error</div>
        }

        <form asp-page="/Account/ForgotPassword" method="post" class="auth-form">
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <label>
                Email
                <input asp-for="Input.Email" class="form-control" autocomplete="email" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </label>
            <button type="submit" class="btn btn-primary">Gá»­i liÃªn káº¿t</button>
        </form>

        <p class="auth-switch">ÄÃ£ nhá»› máº­t kháº©u? <a asp-page="/Account/Login">ÄÄƒng nháº­p</a></p>
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

