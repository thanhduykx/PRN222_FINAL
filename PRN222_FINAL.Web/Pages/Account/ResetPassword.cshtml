@page
@model PRN222_FINAL.Web.Pages.Account.ResetPasswordModel
@{
    ViewData["Title"] = "Äáº·t láº¡i máº­t kháº©u";
    ViewData["BodyClass"] = "auth-body";
}

<section class="auth-page">
    <partial name="_AuthStars" />
    <div class="auth-panel">
        <p class="eyebrow">Password reset</p>
        <h1>Äáº·t láº¡i máº­t kháº©u</h1>
        <p class="subtitle">Táº¡o máº­t kháº©u má»›i cho tÃ i khoáº£n cá»§a báº¡n.</p>

        <form asp-page="/Account/ResetPassword" method="post" class="auth-form">
            @Html.AntiForgeryToken()
            <input asp-for="Input.Token" type="hidden" />
            <input asp-for="Input.Email" type="hidden" />
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <label>
                Máº­t kháº©u má»›i
                <input asp-for="Input.Password" class="form-control" autocomplete="new-password" />
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </label>
            <label>
                Nháº­p láº¡i máº­t kháº©u má»›i
                <input asp-for="Input.ConfirmPassword" class="form-control" autocomplete="new-password" />
                <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
            </label>
            <button type="submit" class="btn btn-primary">Äáº·t láº¡i máº­t kháº©u</button>
        </form>

        <p class="auth-switch"><a asp-page="/Account/Login">Quay láº¡i Ä‘Äƒng nháº­p</a></p>
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

